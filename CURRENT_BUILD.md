# Current Build

## Authoritative Implementation

The current authoritative executable build is the Godot 4 + C# project under `src/societies/`.

- Project: `src/societies/project.godot`
- Main scene: `src/societies/scenes/main.tscn`
- C# project: `src/societies/Societies.csproj`
- C# solution: `src/societies/Societies.sln`
- Current default branch in this repository: `master`

This is the only in-repo implementation that currently has:

- a tracked engine project
- a tracked main scene entrypoint
- a buildable C# project
- a runnable automated validation path

## Current Snow Globe OpenRouter finish-diagnostic refinement

Future raw-free OpenRouter evidence now distinguishes all nine existing finish-admission failures: invalid choice index; missing, wrong-type, or non-`stop` finish reason; choice error present; wrong-type or non-`stop` native finish reason; non-null logprobs; and non-null refusal. Each condition maps through the existing terminal evidence path as `provider_response_rejected_<parser-code>` without retaining the provider value, response body, error/refusal text, exception detail, or secret. The prior short-circuit order and all accepted-response behavior are unchanged.

Historical `provider_response_rejected_response_finish_invalid` artifacts and journal records remain canonical and readable even though new parsing no longer emits the broad typed code. V1 evidence/journal schemas and JSON shapes are unchanged; v1/v2 run results, local validation, and CLI output continue carrying the bounded terminal identity. First-terminal stop, no retry/fallback/alternate, unknown submission/charge, zero trusted tokens and local settlement, null proposal, and response-digest behavior are also unchanged. The zero-I/O readiness manifest's schema, output, bounds, and digest remain frozen.

Red-first Release evidence failed all nine new expectations as the prior broad code, then passed after the split. Final evidence passes 19/19 production-bridge cases, 14/14 finish-parser cases, 5/5 historical/exhaustive-vocabulary cases, 11/11 v2/validation/CLI cases, 4/4 readiness-manifest cases, 99/99 focused evidence/journal/HTTP cases, 97/97 full OpenRouter CLI/security tests, and the full 1006/1006 Snow Globe suite. Both Release builds pass with 0 warnings / 0 errors and `git diff --check` is clean. Independent deep security/public-contract review is FINAL GO with no P0-P3 findings. See [the outcome contract](planning/active/snow-globe-openrouter-finish-diagnostic-refinement.md).

This is an offline forward-looking refinement only. It cannot reclassify the immutable sixth-run terminal evidence, reveal that response's exact condition, or prove current live compatibility, provider quality, or cost. No provider, network, credential, account, paid, retained-evidence, live-state, CLI stage, gameplay, or `src/societies/` action occurred. Implementation commit `2dff642` was published through [PR #145](https://github.com/dfrer/societies/pull/145); required `build-test-smoke` passed in 4m15s and the PR merged to `master` as `d0c7d09`.

## Current OpenRouter sixth authorized-run outcome

The fresh sixth authority used the exact approved ceiling and route: at most 12 sequential requests, one attempt per slot, no retry or alternate provider, and maximum aggregate charge `$0.018`. From merged source `80d0ab2`, Release rebuilt with 0 warnings/errors; zero-I/O `plan`, live `preflight`, paid `record-once`, and local `validate` were each operator-invoked once and are consumed. Plan/preflight/record/validate completed in 149 / 1,268 / 4,913 / 194 ms. Durable state proves exactly one completed preflight generation, one execution claim, and one validation claim; it cannot prove rejected duplicate invocations or the marker-free plan count.

Preflight accepted authorization `16892f25b9c6fe81fcee1911295634f9cc88cf8bff633aa66678bef65fa42574`, artifact `776fe3f1efe6548023328babd4391bdf58d8d5d9cd447896e6c9048eeee6c5fa`, and generation `g2-b85c3da641755eda2f07bd65978ea779ef3caebf8136537f55209d2c6f7d8e6c`. It accessed the fixed anchor/stored credential and performed the contract's three authenticated metadata checks, with no paid inference. `record-once` then made one bounded Azure-only ZDR exchange and stopped terminal with `provider_response_rejected_response_finish_invalid`; there was no second admit/dispatch, retry, fallback, or alternate. This identifies the closed local finish-validation family, not the provider's exact finish-reason value.

The terminal evidence is 2,219 bytes/SHA-256 `8ed4027c13f90eff69600c694d0f7d03bb316fd8bc936edbd66e20c4dc644366`: one exchange, `SubmissionUnknown` / `ChargeState.Unknown`, zero trusted tokens, zero locally settled microusd, null proposal, and a nonzero response digest. Local validation accepted receipt SHA-256 `c1416abc97142356808e57f9d57369672e7845fbc5d5ba6521e487a92e3d6c41`. The canonical four-record journal is 2,857 bytes/SHA-256 `9455a71f7b8e38a2aa389e09a41c139e7060de934e9446857b8902f194fb6f3f`, ending checksum `8ce629857708749059669ffebf93fb31af875223870c353efac6adadd9b76688`; both execution and validation tombstones exist. Charge remains unknown and zero local settlement is not a zero-cost claim. No raw prompt/response/secret, account query, accounting follow-up, credential mutation, state reset, or `src/societies/` change occurred. Independent deep security/evidence review is FINAL EVIDENCE GO with no P0-P3 findings. Evidence-record commit `ce217a6` was published through [PR #143](https://github.com/dfrer/societies/pull/143); required `build-test-smoke` passed in 8s and the PR merged to `master` as `e8df0c1`. The sixth authority and every stage are consumed and must not be rerun.

## Current Snow Globe OpenRouter raw-free rejection-diagnostics milestone

The isolated OpenRouter boundary now preserves the exact closed local parser rejection code in future terminal evidence as `provider_response_rejected_<parser-code>`. The diagnostic flows through the existing slot receipt, durable journal, evidence artifact and local validation, v1/v2 production results, and CLI output. One typed parser enum is both the construction authority and wire suffix vocabulary; undefined enum values fail closed, and exhaustive coverage prevents a future parser rule from silently falling back to the generic code. Provider text, response bodies, and exception messages are not retained.

Admission and financial semantics are unchanged: the first terminal exchange stops the run with no retry/fallback/alternate, `SubmissionUnknown` / `ChargeState.Unknown`, zero trusted token and local-settlement counters, null proposal, and the existing response digest. Pre-parser policy and unexpected exchange failures remain generic. V1 evidence/journal schema identities and JSON shapes are unchanged, and historical generic `provider_response_rejected` evidence remains valid and byte-for-byte untouched. The fifth attempt's discarded parser cause cannot be reconstructed, so this milestone does not prove current live compatibility or readiness for another paid attempt.

Red-first fake evidence passed the three unchanged transport cases and failed all eight parser-diagnostic cases as generic. Final Release evidence passes 12/12 targeted bridge/CLI cases, 88/88 evidence/journal/HTTP tests, 80/80 full OpenRouter CLI/security tests, and the full 995/995 Snow Globe suite. Both Snow Globe and OpenRouter CLI Release builds pass with 0 warnings / 0 errors; `git diff --check` is clean. Independent deep security/public-contract re-review is FINAL GO with no P0-P3 findings after replacing a drift-prone manual allowlist with the typed exhaustive vocabulary. Implementation commit `3e8ba7a` was published through [PR #141](https://github.com/dfrer/societies/pull/141); required `build-test-smoke` passed in 4m16s and the PR merged to `master` as `ba161f9`. See [the outcome contract](planning/active/snow-globe-openrouter-raw-free-rejection-diagnostics.md). No provider, credential, account, network, paid, retained-evidence, live-state, preflight, `record-once`, `validate`, `src/societies/`, or gameplay action occurred.

## Current Snow Globe persisted-inspector pinned read-scope milestone

The isolated lab now has a shared internal pinned read scope for `SnowGlobePersistedRunInspector.Inspect`, `InspectRecoveryProvenance`, and `InspectDurableControlStatus`. One call lexically canonicalizes the finite run-store path, validates existing directory components root-to-leaf so a linked ancestor is rejected before any descendant metadata/access, validates direct entries, opens each schema-consumed artifact exactly once with read-only `ReadWrite|Delete` sharing, and makes both strict RunStore parses read those same handles from offset zero under the unchanged bounds. Names and link policy are revalidated after opening and after the second parse; `.writer.lock` remains permitted but unopened. Malformed typed headers, including the prior `JsonException` path, normalize to `run_store_invalid` with unconditional handle cleanup.

Public signatures, receipts, schemas, rejection vocabulary, v2-v5 reconstruction, optional continuation behavior, v4 recovery provenance, v5 durable status, and inert snapshot semantics are unchanged. A byte-identical pathname replacement cannot become the second evidence source; observed in-place byte drift, layout drift, or link-policy drift fails `run_store_unstable`. This remains read-only evidence, not path ownership: current-path binding at return, directory-object binding, link swap-and-restore, hard-link exclusion, same-object content ABA protection, native file IDs, writer authority, and after-return stability are not claimed.

Final local evidence passes 46/46 focused inspector tests, 234/234 inspector/persistence compatibility tests, and the full 992/992 Snow Globe Release suite; both Snow Globe Release projects build with 0 warnings / 0 errors and `git diff --check` is clean. Tests cover one-open/two-offset-zero-read behavior, accepted/rejected disposal, pinned-original replacement drift, header/ledger/marker mutation, layout/link-policy drift, root-to-leaf/no-descendant ancestor rejection, typed-header cleanup across v2-v5 and all three surfaces, trailing-separator compatibility, conditional physical symbolic-link rejection, no lease/mutation, concurrent valid append/Pause/Resume, and unchanged receipt/reconstruction behavior. Independent security/determinism/public-interface re-review is FINAL GO with no P0-P3 findings after closing two P1s in ancestor-validation order and malformed typed-header cleanup. Implementation commit `8ce9214` was published through [PR #139](https://github.com/dfrer/societies/pull/139); required `build-test-smoke` passed in 4m20s and the PR merged to `master` as `980fc52`. See [the outcome contract](planning/active/snow-globe-persisted-inspector-pinned-read-scope.md). No provider, credential, network, paid, live-state, retained-evidence, dependency, `src/societies/`, or gameplay behavior changed.

## Snow Globe v5 durable-pause milestone

The isolated Snow Globe lab now has a local implementation draft for `snow_globe_run_store/v5`. V5 adds one checksum-linked `PauseTransition` frame to the existing framed journal so `SnowGlobePersistedSession` can restore the last committed pause state after close/reopen without changing deterministic world state, events, or digests. Public three-argument creation starts running; four-argument creation with `true` commits Pause before return. Three-argument reopen derives v5 pause, while the Boolean reopen overload remains v4-only and rejects v5 after read-only preflight but before writer ownership.

V2/v3 remain strict flat read-only inputs. Explicit `V4SchemaVersion` routes frozen v4 through its unchanged append, participant, scheduled-recovery, recovery-provenance, and ephemeral-pause contracts. V5 pause recovery reuses the same one-continuation/two-segment bound and always abandons an uncommitted pause frame at the prior durable state after validating any complete payload; partial payload/commit evidence fails closed. Persisted-run inspection remains inert (`Snapshot.IsPaused=true` for every schema), and recovery-provenance receipts remain v4-only.

Local Release evidence passes 25/25 new v5 pause tests, 186/186 persistence compatibility tests, the 239/239 aggregate persistence/provider-enum selection, and the full 969/969 Snow Globe suite; the Snow Globe Release library build has zero warnings/errors. `git diff --check` is clean, and independent migration/determinism/public-interface review is FINAL GO with no P0-P3 findings. Implementation commit `9b4b1c4` was published through [PR #135](https://github.com/dfrer/societies/pull/135); required `build-test-smoke` passed in 4m34s and the PR merged to `master` as `6505e01`. See [the v5 contract](planning/active/snow-globe-v5-durable-pause.md). No `src/societies/`, provider, credential, network, paid, live-state, world-event, or observer-shell behavior changed.

## Current Snow Globe v5 durable session-control status milestone

The isolated lab now has an additive `SnowGlobePersistedRunInspector.InspectDurableControlStatus(directory, expectedIdentity)` implementation. After the same strict exact-identity and two-read evidence-stability gate as existing inspection, a v5 run returns a detached raw-free `snow_globe_persisted_session_control_status_receipt/v1`: `Running` or `Paused`, bound to the run-identity checksum, stable evidence checksum, and committed tick/event-count/state/event digest tuple. This is the distinct durable reopen state surface; ordinary `Inspect(...).Snapshot.IsPaused` remains inert and is not historical pause evidence.

The v5 state is reconstructed from existing validated transition evidence, so a valid uncommitted pause frame reports the prior durable state. Partial/noncanonical evidence fails closed. V2/v3/v4 remain accepted but receipt-free; no running state is fabricated for them. The implementation changes no storage wire contract, writer/session mutation, v4 ephemeral pause, recovery provenance, path handling, provider, network, credentials, live state, or `src/societies/`. Focused Release `PersistedRunInspectorTests` pass 30/30, v5 pause/session tests pass 25/25, the full Snow Globe Release suite passes 976/976, and the Snow Globe Release library build passes with 0 warnings / 0 errors. `git diff --check` is clean, and independent determinism/persistence/public-interface review is FINAL GO with no P0-P3 findings. Implementation commit `5a7bf43` was published through [PR #137](https://github.com/dfrer/societies/pull/137); required `build-test-smoke` passed in 4m16s and the PR merged to `master` as `4683bba`. See [the outcome contract](planning/active/snow-globe-v5-durable-session-control-status.md).

## Current Snow Globe v4 recovery-provenance receipt milestone

The isolated Snow Globe lab now has a second, deliberately read-only persisted-run inspection surface: `SnowGlobePersistedRunInspector.InspectRecoveryProvenance(directory, expectedIdentity)`. After the same exact expected-identity and two bounded raw-evidence reads used by `Inspect`, an accepted v4 run returns a detached, raw-free receipt. It distinguishes `NoDurableRecovery`, `AbandonedIncompleteScheduledTick`, and `AdoptedCompleteScheduledTick`. A durable disposition and source binding exist only when the existing strict v4 reader has already validated the continuation marker against the prepared source frame; the receipt carries the run identity checksum, exact-read evidence checksum, committed tick/event/state/event identity, continuation checksum, and bounded source segment/frame/length/checksum fields. A merely pending tail remains `NoDurableRecovery`; v2/v3 can be accepted but return no v4 receipt.

The surface neither acquires a writer lease nor opens/appends/repairs/continues a run, and it does not invoke an adapter or alter deterministic-world authority. Required marker properties reject JSON null before typed validation, preventing malformed continuation evidence from escaping the stable no-receipt failure contract. Focused Release evidence passes 23/23 `PersistedRunInspectorTests` and 58/58 inspector/RunStore-v4-crash-recovery/persisted-session-v4-recovery tests; the full Snow Globe Release suite passes 943/943; and the Snow Globe Release build passes with 0 warnings / 0 errors. `git diff --check` is clean, and independent determinism/public-contract review is FINAL GO with no P0-P3 findings after closing the null-marker P1. Implementation commit `10b9816` was published through [PR #133](https://github.com/dfrer/societies/pull/133); required `build-test-smoke` passed in 4m12s and the PR merged to `master` as `11d662b`. No `src/societies/`, provider, credential, payment, network, live-state, or mutable-session behavior changed.

## Current Snow Globe read-only persisted-run inspection milestone

The isolated lab now exposes one deep read-only interface: `SnowGlobePersistedRunInspector.Inspect(directory, expectedIdentity, eventCursor)`. A caller can inspect strict v2, v3, or v4 run evidence without supplying an inference adapter or acquiring writer ownership. The exact expected identity is mandatory; the inspector performs two bounded raw-evidence reads, rejects identity or artifact drift, reconstructs only committed deterministic state, and returns the existing detached observer snapshot with at most 32 events. `IsPaused=true` means the returned projection is inert, not that historical pause state was persisted.

V4 evidence binds the raw header plus every ledger/marker segment; v2/v3 evidence binds exact raw header and ledger bytes with explicit length framing. Pending v4 prefixes or complete uncommitted payloads remain at the prior committed checkpoint, and inspection never creates a lease, append, continuation, repair, adapter call, or artifact mutation. Failures return stable reasons with no partial snapshot or parser/filesystem detail. The public `SnowGlobeRunStore.Read` behavior is preserved by delegating through the same internal evidence-bearing reader.

Final local Release evidence is 12/12 inspector tests, 143/143 inspector/RunStore/v4-recovery tests, 175/175 persistence compatibility tests, and 932/932 full Snow Globe tests. The Release build has 0 warnings and 0 errors, diff checks are clean, and independent determinism/public-interface review is GO with no P0-P3 findings. The full gate also exposed and resolved checkout-dependent CRLF in a frozen test fixture (`WI-SOCIETIES-2026-012` / `WI-GLOBAL-2026-131`) without changing the production canonical manifest. No `src/societies/`, provider, credential, paid, network, live-state, or mutable-session behavior changed. Implementation commit `419af90` was published through [PR #131](https://github.com/dfrer/societies/pull/131); required `build-test-smoke` passed in 3m50s and the PR merged to `master` as `61c746e`.

## Prior Snow Globe persisted-session v4 recovery conformance milestone

The unchanged public `SnowGlobePersistedSession.Reopen` path now has adversarial conformance evidence over physical RunStore v4 interruption artifacts. Six Release tests prove that a mutable session durably records recovery before it is returned, preserves original segment bytes, reconstructs the exact public snapshot and idempotent participant receipts, continues deterministic progress, remains stable on repeat reopen, rejects malformed UTF-8/NUL/garbage before writer ownership, and rejects a second recovery without creating a third segment. The internal filesystem seam is used only to compose interruption artifacts; recovery remains RunStore-local and no session recovery overload was added.

The aggregate gate also exposed and resolved a test-only Ollama benchmark race: fake successful POSTs used scheduler timing to imply exactly one in-flight VRAM sample, which could yield zero or an extra sample under full-suite load. An opt-in per-request POST/probe handshake now makes those tests deterministic without changing production inference, provider, network, or sampling behavior. This is recorded as resolved workflow issue `WI-SOCIETIES-2026-011` / `WI-GLOBAL-2026-130`.

Final local Release evidence is 6/6 new recovery tests, 163/163 PersistedSession/RunStore tests, 76/76 Ollama benchmark tests, and 920/920 full Snow Globe tests. The Release build has 0 warnings and 0 errors, diff checks are clean, and independent recovery/determinism review is FINAL CODE GO after closing all P1-P3 findings. Only isolated-lab tests and v4/v2-v3 wording changed; no `src/societies/`, provider, credential, paid, live-state, or production recovery behavior changed. Implementation commit `11f7fd2` was published through [PR #129](https://github.com/dfrer/societies/pull/129); required `build-test-smoke` passed in 3m55s and the PR merged to `master` as `2edfbe6`.

## Prior Snow Globe RunStore v4 recovery milestone

New isolated-lab run stores now use `snow_globe_run_store/v4`. The public `CreateNew` / `OpenForAppend` / `Read` surface is unchanged, while the storage module writes bounded checksum-linked prepare, payload, commit, and continuation evidence. An ordinary read exposes committed frames only. After one deterministically interrupted scheduled-tick write at an authenticated complete-record boundary, `OpenForAppend` can durably record either abandonment at the prior checkpoint or adoption of the complete tick in a new continuation segment without truncating or rewriting the original segment. Participant-command tails, a second recovery, malformed or unauthenticated residue, corrupt committed history, broken or forked links, header changes during lease acquisition, unknown artifacts, and combined capacity above 4,096 entries fail closed.

V2 and v3 artifacts remain strict read-only inputs and are never upgraded in place; frozen scheduled and participant v3 fixtures reconstruct successfully. The filesystem and fault-injection adapters remain internal, and `src/societies/` is unchanged. This proves deterministic record-boundary interruption and ordinary restart behavior only. A real partial filesystem write fails closed; there is no power-loss, hardware-durability, cross-host, general-transaction, or exactly-once claim.

Local Release validation passes 132/132 focused RunStore tests and 914/914 full Snow Globe tests; the Release build has 0 warnings and 0 errors; `git diff --check` is clean. Independent migration/determinism review is FINAL CODE GO with no P0-P3 findings after correcting premature uncommitted-tick visibility, global-cap enforcement, authenticated-prefix recovery, header/lease binding, framing-aware adversarial tests, and continuation source lengths. Implementation commit `a41ea6b` and delivery record `f7aa9d5` were published through [PR #127](https://github.com/dfrer/societies/pull/127); required `build-test-smoke` passed in 4m17s and the PR merged to `master` as `16239d3`.

## Historical OpenRouter production settlement and fifth paid-run outcome

The fifth authority incorporated the exact terms: up to 12 sequential requests, no retries or alternate providers, and maximum aggregate charge `$0.018`. One session ran exactly once: preflight succeeded, `record-once` ran once, and local `validate` ran once. Authorization digest is `40655d2535520757b411595593deda6a714127e5366cf9afb3292aab0f3bc2d6`; generation is `g2-03d00c2b41770f885e7ce27c9a4545cd9a420161b00351573fc2886b6c886df2`, manifest SHA `cd71868d349bb71b88d5b819a14e166fb520055d706f6da95025d04f33325d83`.

The raw-free evidence is 2,171 bytes, SHA-256 `7d449d77c3b82ff1984a0e1d33c3026b80566321938418b2eb363ff1aa9f1bd8`, terminal `provider_response_rejected`, exchange count 1, `cq1` only, `SubmissionUnknown` / `ChargeState.Unknown`, zero trusted prompt/completion/total tokens, zero locally settled microusd, and null proposal. The 793-byte receipt SHA-256 is `7f42dfd15ffe1e4134d7110a9f491f9dfaae1632d69e14180bf47dbc6056dc49`; it binds the same authority, generation, manifest, state, and evidence and records `additional_attempt_authorized=false`. Journal SHA-256 is `b79acbe2cff7b2a72bee53db5c3663229de035f3700c5624b5d6f2a2dcffa161`, with four records and terminal checksum `e3b987863489740096370a00a3b34374e16e7ab59d817e59743ddf2833f7b5b3`: consume preflight, admit slot 1/reserve 1500, dispatch unknown, complete rejected; no second admit or dispatch, append closed, both tombstones present.

The authority is consumed and no stage may be rerun. Preflight performed authenticated metadata verification and produced durable authorization, but the exact metadata count is not retained here. No raw prompt/response/secret was retained, no provider/accounting follow-up occurred, and v1 was not accessed. Local zero settlement is not provider zero cost; provider charge is unknown. The evidence does not retain HTTP status, finish reason, response body, redirect trace, or parser-rejection detail, so no provider-side cause is claimed. Independent deep review is GO with no P0-P2; no runtime/framework Bearer cleanup proof is claimed.

### Current delivery record and next boundary

The reviewed Snow Globe/OpenRouter milestone was committed as `dfbeb81`, published as [PR #125](https://github.com/dfrer/societies/pull/125), and merged to `master` as `f021cdc`. GitHub `build-test-smoke` passed twice on the delivery branch, including the authoritative Godot build, fast unit tests, Godot C# builds, and headless smoke suite. The merged PR contains no `src/societies/` change.

The residual P3 is closed by commit `399adac` in [PR #126](https://github.com/dfrer/societies/pull/126): fake lease evidence now retains and validates all twelve success observations plus exact uncertain and pre-dispatch observations, rejecting missing, null, extra, false, or count-mismatched evidence. Focused Release tests pass 10/10, full SnowGlobe Release passes 885/885, CLI/security passes 70/70, both Release builds have zero warnings/errors, and independent deep security review is GO with no P0-P3 findings. PR #126 merge completes this follow-up; it authorizes no credential, live-state, provider, network, accounting, paid, or `src/societies/` action.

## Historical fourth-authorization preflight outcome

The authoritative OpenRouter state is the Windows production bridge and CLI described by the [focused contract](labs/Societies.SnowGlobe/OPENROUTER_PREMIUM_EVIDENCE.md) and [ADR 0015](docs/adr/0015-offline-openrouter-premium-evidence-boundary.md). It now includes a zero-I/O `plan` command backed by raw-free bounded manifest `snow_globe_openrouter_paid_run_readiness_manifest/v1`, digest `d1e653468fd7a39e33ad355297adf96c48bde97e40a73f6b6c6812623553f737`. The dedicated offline entrypoint runs before production-factory dispatch, including invalid `plan` arguments. The manifest pins `openai/gpt-5.6-luna` / `openai/gpt-5.6-luna-20260709`, Azure-only ZDR routing, twelve sequential one-attempt slots with no retry or alternate, 4,096/512 input/output tokens, minimal excluded reasoning, required finish reason `stop`, 15-second leases, a 240-second/key-expiry window, 1,500/18,000-microusd ceilings, stop on terminal or uncertain outcome, and fresh explicit authority.

The manifest inspects all twelve real canonical requests and runs a cached fake-only, in-memory conformance probe through the real evidence executor. It observes twelve sequential successes, a one-call uncertain stop, a pre-dispatch stop, consumed-capability and duplicate-nonce closure before further calls, no retry/alternate path, and lease-buffer zeroing. These fake capabilities and nonces are process-local conformance evidence only; they create no live, durable, paid, provider, account, credential, or production authority. The later authorized preflight exposed the limit of that evidence: it did not cover fixed production state-root generation rollover.

On 2026-08-22 the user granted the exact fourth authority: up to twelve sequential requests, no retries or alternate providers, and maximum aggregate `$0.018`. The readiness digest, branch/HEAD, dirty inventory, diff check, and unchanged `src/societies/` boundary were reconfirmed. Exactly one `preflight` invocation then returned `OPENROUTER_FAILED code=preflight_already_attempted`, observed exit 1. The fourth authority is consumed by that terminal preflight attempt and must never be retried.

Production preflight checks existing `preflight-failed`, runtime-authorization, and `execution-consumed` markers before `_metadataVerifier.VerifyOnceAsync`. It therefore made no metadata GET, credential read, new activation bundle or authorization, provider/API/account/inference request, paid request, charge, or new artifact/state mutation. No `record-once` or `validate` invocation occurred for Attempt 4.

Three paid authorities were separately consumed exactly once on 2026-08-21. Attempt 1 made one Azure HTTP 200 request and cost `$0.000158`; local admission stopped as `provider_response_rejected`. Its archived 2,171-byte artifact SHA is `5114c02ec887ffd459ec254ed5e61daea7620638ec1fd7817a132c0c1238b315`, with validation receipt `569bdc87cf04adfe4d48ef4d8f755edb5047c69451c214b6cdad27c65be5ea58`.

Attempt 2 used authorization digest `11836bee33f6e3008894c2e426889b67123a0853a99304c7e5439c0a6eaa3fc2` and preflight artifact digest `f54eacfba80ee6fb9099260eacc8f286837e74e80c70f7dd8f9261854bcc1508`. Exactly one paid invocation stopped as `provider_timeout` under the then-current 5-second lease. Its archived 2,151-byte artifact SHA is `0948bc8d4e6a91427ce49b32299cf2dab40128bc53d80c2968dd61c12ed0564f`, with validation receipt `2d3ea8b73ff737b1354fb384a783bbf4749299b022887581075c42ecb4cd7439`. Upstream evidence shows one Azure HTTP 200 attempt, but no corresponding generation/accounting row was retained when checked, so the charge is unresolved.

Attempt 3 used authorization digest `d13f4aaa9f930f27923147ef7ddcadbcefaf5938ca7217698fb8aa477a683288` and preflight artifact digest `60e8f0d1b07b6ed7c8f191d94add6f2187035cbc7f6d9492d95dc0ec7c7e19ad`. Exactly one paid invocation stopped as `provider_response_rejected`; its retained 2,171-byte artifact SHA is `af485def08d5c465807d069fc59a566c077f111b003723c0ece75b8db7732ea0`, with validation receipt `0885c1b530d7a12b27cbe6c7c5eddc6dd27957125fe1650d741f70a4c0332ba3`. OpenRouter account evidence records Azure, 436 input tokens, 128 output tokens, finish reason `length`, and cost `$0.000241`. No attempt retried or produced a proposal, score, or comparison; local zero settlement is never a zero-cost claim.

Attempt 1 exposed UTC-date and response-parser drift; attempt 2 exposed undersized lease/aggregate timing; attempt 3 proved that the 128-token completion cap could truncate a structured response. The corrected request uses `max_completion_tokens=512`, minimal excluded reasoning, authenticated reasoning-support checks, and unchanged strict `finish_reason=stop` admission. The per-slot/aggregate ceilings are 1,500/18,000 microusd so the full 4,096-input/512-output exposure of 1,434 microusd fits before dispatch; the 15-second lease and 240-second/key-expiry boundary are unchanged.

The completed offline v2 production-composition milestone adds a fixed read-only 32-byte domain-separated HMAC anchor source at a distinct Windows Credential Manager target. It is open-existing only: no provision, replace, delete, rotate, import, or scan; leases and owned buffers are disposable and cleaned. Anchor validation precedes root, capability, credential, metadata, and exchange access. The exact fixed `%LOCALAPPDATA%\\Societies\\SnowGlobe\\OpenRouterPremiumOneShot\\v2` root never observes or falls back to v1, and exposes exactly three v2 state operations. Credential store/delete remain separate and do not open the state anchor or v2 root. Validation requires the exact digest and O(1) lookup; store-only frozen-journal reopen follows the durable execution claim, with canonical restart readback and authenticated final journal/evidence binding. Post-claim indeterminate state is permanent and never retried. Mutation-blocking `GENERIC_READ`/`FileShare.Read` ancestor and fixed-directory leases close junction/reparse/path-swap windows; ordinary pin behavior is unchanged.

The previously reviewed composition baseline passed 55/55 focused, 868/868 full SnowGlobe Release, and 67/67 CLI/security tests with clean Release builds. The exact `state-anchor-provision-once --acknowledge-create-fixed-v2-state-anchor-and-initialize-offline` command was then invoked once under fresh explicit local authority and exited 0. It generated and stored the separate anchor without displaying or logging its secret, and initialized the fixed v2 root with empty `authorities`, `execution-consumed`, `generations`, and `validation-consumed` directories plus `root-writer.lock` (567 bytes, SHA-256 `7827eeb9d15d1b43eeaaebe779b8d192ac3864e25fbfe2df8ea57b8572fd88aa`). The safe anchor identity digest is `d6eb68c6f14e32f342caee45f4c13d2398e4c0830e39d3e7cbfe478a10d9a78d`; the state-contract digest remains `f9d12a2c0bcfb60cc874dc49bc60462197700d681a42ae98ce4bcefd28ac8511`. The provisioning authority is consumed and the command must never be rerun. No OpenRouter credential read, provider access, paid action, fifth authority, generation, claim, evidence, or receipt occurred; v1 was not accessed. Local files and the credential anchor do not claim owning-user/admin or whole-volume rollback resistance; rotation and recovery are not implemented.

The CLI `plan` remains pre-factory zero-I/O: it exited 0 with one bounded raw-free line, `trust_anchor_status=not_inspected`, `production_factory_constructed=false`, `additional_attempt_authorized=false`, and `live_readiness=false`. Manifest v2 digest is `eea26a92d318a2ba102c7979d0cb44563d8bef967ae00b627bc6263ff59d759d`; Attempt 4's historical v1 digest remains `d1e653468fd7a39e33ad355297adf96c48bde97e40a73f6b6c6812623553f737`; state-contract digest is `f9d12a2c0bcfb60cc874dc49bc60462197700d681a42ae98ce4bcefd28ac8511`.

The trust-anchor abstraction remains externally supplied and the secret remains in Windows Credential Manager. Local files alone do not protect against an owning user/admin or whole-volume rollback; no rotation or recovery path exists. Anchor provisioning is complete for this fixed local v2 target, but this is not a live-readiness claim.

Live fixed-v1 state remains immutable Attempt 3: runtime authorization `d13f4aaa9f930f27923147ef7ddcadbcefaf5938ca7217698fb8aa477a683288`, preflight artifact `60e8f0d1b07b6ed7c8f191d94add6f2187035cbc7f6d9492d95dc0ec7c7e19ad`, evidence `af485def08d5c465807d069fc59a566c077f111b003723c0ece75b8db7732ea0`, and validation receipt `0885c1b530d7a12b27cbe6c7c5eddc6dd27957125fe1650d741f70a4c0332ba3`; hashes still match, and execution-consumed and validation-consumed tombstones remain. Do not move/delete this state, reuse its digest, or call `record-once` or `validate` against it.

At the pre-delivery fixed point, all OpenRouter code, tests, and documentation were dirty/untracked on `feature/snowglobe-agent-lab-owner-v1` at HEAD `246304f`; nothing had been staged, committed, pushed, or opened as a PR, `git diff --check` passed, and `src/societies/` was unchanged. That fixed point was later committed and published through PR #125 as recorded above. The credential intentionally remains in Windows Credential Manager and was not read or displayed. The v2 lifecycle initialization is complete; existing `WI-GLOBAL-2026-126` remains the separate public-documentation-traffic issue.

Initialization-slice chronology: the implementation gate passed full SnowGlobe 881/881 and full CLI/security 70/70. A dedicated real Global mutex abandonment regression in `OpenRouterPremiumWindowsStateTrustAnchorTests` proves fail-closed `state_trust_anchor_provisioning_indeterminate`, releases recovered ownership, and permits later reacquisition. The later lease-zero conformance follow-up closes the former residual P3 with focused 10/10, full SnowGlobe Release 885/885, full CLI/security 70/70, and both lab and CLI Release builds at 0 warnings/0 errors; independent deep security review is GO with no P0-P3.

### Historical next action

The fifth paid-run authority is consumed. Its documentation-and-delivery-review action is complete and superseded by the current Git-delivery action stated above.

## Current offline cognition score-summary milestone (v4)

### Attempt-005 completed local v4 evidence

Offline comparison is now complete/historical. It evaluated benchmark source 3,618 B SHA `961b54b7d8cfb2aead566579499adb3aa21f1d85bfbe0b7c6fc504a8adc40e0d` against v4 source 16,148 B SHA `fecf71cbe8cc268dadb603d29735a816bc0152ccc79b4ea44c5a91d7e7616d3e`. The in-memory report is 8,916 B SHA `19f7053418471c8c70bdb9fffbfcca042f5bd87c24796a28227a672558990e56`, payload `3c3ff4a3e97344afb80d2a6283827e3d846c73e0b7730765c5d09601db6d4acc`, status `insufficient_live_premium_evidence`; all four premium fields are null. Results are 12 scenarios, 262/1200 (2183 bp), dispositions 0/7/2/1/2, tuples 0/0, 0/0, 62/2066, 200/6666. No report file, provider, network, live traffic, or workspace mutation; deep review FINAL COMPARISON GO, no P0-P2, exact bindings.

Historical/superseded next action: offline Design-It-Twice for the first live-premium evidence/profile boundary. It was settled by `ef32576`; see the current OpenRouter boundary above.

Committed HEAD `2e398a03a365ee259e6a31d25fbb83b6712f592f` completed the authorized v4 action. Preflight/record/validate ran exactly once and exited 0 in 141/26,420/175 ms; record was `Complete`/`None`, 12/12, plan digest `6c70ed6d69c378eb1fcfbc744dacdb4af41085cb57eaac42b4ee45e1ebd333b4`. Artifact path is `artifacts/snowglobe/local-model/qwen3.5-4b-recording-execution-v4.json`, 16,148 B, SHA `fecf71cbe8cc268dadb603d29735a816bc0152ccc79b4ea44c5a91d7e7616d3e`; receipt `86ccd0b468a1b633f386b0abbe90386695f994c40be47f24dcffd63867529d65`; summary `1958e8b6c4601c9a9e9834403cd431a67a48324735dca7484d10809509245a9a`; report `0e5fe1d7a8849caf7294c79c5c80863db0aa7521fb3b7e991b469867538a4fe1`. Score 262/1200 (2183 bp), dispositions 0/7/2/1/2, cq1..cq12 valid, 12 HTTP 200 POSTs, no retry/alternate/fallback/download/update. Deep review FINAL EVIDENCE GO, no P0-P2; cleanup is operator-observed only. v1/v2/v3 unchanged.

The bounded offline local-premium comparison and its former Design-It-Twice action are complete and historical. The former OpenRouter preflight action is complete/historical; the current decision is stated above.

The authoritative documentation state is commit `9bb4027`, **Add bounded cognition score summary**, atop v3 evidence commit `38c9bdb`. The internal fixed raw-free codec embeds the canonical quality report once and retains no prompts, responses, proposals, submissions, or model text. It enforces exact scoring tuple checks, including scenario-specific lower-utility reachability, bounded dispositions, and detached results. For a complete result, only the score summary is populated; terminal and `EvidenceRejected` are null. This is structure/integrity evidence, not authenticity, premium quality/cost/winner, or general-intelligence evidence. No historical backfill has occurred.

The v4 schema identities are plan `snow_globe_ollama_recording_composition_plan/v4`, receipt `snow_globe_ollama_loopback_recording_receipt/v4`, and artifact `snow_globe_ollama_recording_execution_artifact/v4`; the retained artifact path is `artifacts/snowglobe/local-model/qwen3.5-4b-recording-execution-v4.json`. Transport remains v3 and the high-level adapter/profile remains v2. The receipt is canonical digest-only and the artifact embeds the summary once. `LocalPremiumComparison`'s two-input overload emits local cognition facts only (`premium`, `premium_cost`, `performance_delta`, and `quality_delta` are null; status `insufficient_live_premium_evidence`); the one-input golden is unchanged.

Validation evidence: red phases `10/23/33`, then `6/92/98`; final targeted `98/98`, owned `157/157`, full SnowGlobe `706/706`, Recording CLI `59/59`, Benchmark CLI `56/56`, and three Release builds with 0 warnings/errors. Independent deep review was FINAL CODE GO with no P0-P2 findings. Snapshot `21c3f18942072aaa6954a6f95dd86528c33b94386d6263938b906565a66032e8`.

Attempt-001 through Attempt-004 chronology and v1-v3 artifacts remain preserved; older actions are historical/superseded. Attempt-005 and the bounded offline comparison are complete. The former Design-It-Twice action is historical/superseded.

## Product Direction and Current Status

The product north star is: "A deterministic civilization/ecology simulation where humans and AI citizens work, trade, negotiate, govern, and experience shared consequences."

That statement is future intent. Current implementation facts remain limited to the scope below. The deterministic simulation owns facts and all world-changing outcomes; any future LLM layer is limited to interpreting structured state, deliberating, communicating, summarizing memory, and proposing actions. Validated deterministic commands/events remain the only state-change path. Humans must stay consequential, and offline/model-failure fallback must preserve simulation and replay. See [planning/PRODUCT-THESIS.md](planning/PRODUCT-THESIS.md).

Current milestone truth: W2-02 through W2-05 are validated and merged. W2-06 is locally accepted at `478a4d9`. W3-01 merged through PR #122 at master `7b747af`. W3-02 merged through PR #123 at origin/master `d9e297f`. W3-03 implementation is committed locally at `a513636` on `feature/v3-w3-03-wetland-consequences`; docs/evidence publication and GitHub delivery remain pending. W3-04+ remain inactive; another explicit **Continue V3** decision is required before activation.

### a713267 documentation handoff (authoritative local state)

Commit `a713267` is locally committed and not pushed, in a PR, or deployed. It changes exactly six code/test files: `labs/Societies.SnowGlobe/OfflineOllamaRecordingCodec.cs` (codec/profile and internal port), `labs/Societies.SnowGlobe/OllamaLoopbackRecording.cs` (facade/session), `labs/Societies.SnowGlobe/OllamaLoopbackRecordingTransport.cs` (transport/verifier), `tests/Societies.SnowGlobe.Tests/OfflineOllamaRecordingCodecTests.cs`, `tests/Societies.SnowGlobe.Tests/OllamaLoopbackRecordingTests.cs`, and `tests/Societies.SnowGlobe.Tests/OllamaLoopbackRecordingTransportTests.cs`.

Offline validation recorded for this committed slice is security-focused 90/90, full lab 529/529, BenchmarkCli 56/56, Release 0 warnings/errors; independent deep review focused 114/114, full 529/529, CLI 56/56, build 0/0, CODE GO, no P0-P2. No live Ollama/listener/socket/HTTP/process/file hash/model/GPU/provider/credential/payment action occurred. The implementation is not live compatibility, artifact-loaded, model-execution, quality, cost, world-authority, production-readiness, or commercial proof. See [the loopback contract](labs/Societies.SnowGlobe/OLLAMA_LOOPBACK_RECORDING.md) and [ADR 0013](docs/adr/0013-offline-tested-ollama-loopback-recording.md). The prior seven documentation files are already committed; no tests are rerun for this prose-only reconciliation.


## Current Prototype Scope

The current authoritative implementation candidate is validated at `a513636`, on top of merged W3-02 (`d9e297f`) and the merged Prototype V3 W2-05 schema-v7 crisis build. W2-VIS retains its documented explicit waiver; Git/GitHub remains authoritative for delivery/merge state.

Implemented today:

- deterministic local session bootstrap
- heightfield terrain with biome/buildability/movement-cost overlays
- first-person movement and harvesting
- observer camera and runtime overlay cycling
- seeded scenario world generation
- editor-filesystem and exported-PCK catalog loading from the same validated JSON data
- citizen-based settlement simulation with food, fatigue, beds, hearth service, and build queue management
- terrain-aware route planning, built path corridors, remote depots, and logistics metrics
- local JSON snapshot + event-log + run-summary output
- session-owned stable-ID resource ledger with command/result harvesting; its historical schema-v7 fields and compatibility remain retained inside the current strict runtime snapshot schema-v9 persistence and migration contract
- catalog-driven `empty_stores` scenario plus a pure session-owned crisis evaluator with tick-only stable, incapacity-collapse, and deadline holds
- deterministic atomic raw-resource contribution from player inventory to the authoritative central depot, with depot proximity input, exact results, per-resource counters, and crafted-tool exclusion
- domain-owned terminal Stable/Collapsed outcomes and exactly-once terminal events, carried by the current strict schema-v9 crisis checkpoint with the historical schema-v7/v8 crisis fields retained alongside full crisis, contribution, directive, telemetry, and wetland state
- one irreversible protect/draw-down civic policy contract with a typed current-tick/expected-version command, inclusive `0..1200` decision window, and exactly-once stable event
- strict schema-v9 checkpoint, run-summary, and artifact persistence with strict v9 rejection and neutral v5-v8 migration; prior schema-v7/v8 artifacts remain historical compatibility inputs
- procedural crisis HUD with contract time, directive, ordered cumulative contributions, four stability checks, both deterministic holds, and terminal causal summary
- deterministic derived-only citizen preferences and reasons, ordinal capture, relative supports/opposes/uncommitted labels, and an atomic aggregate summary captured after policy selection; legacy schema-v8 zero-summary compatibility and strict present-summary validation remain preserved within current schema-v9
- session-owned deterministic wetland consequences: Protect selects up to 4 reeds at health 75 with -1 health per reed; DrawDown selects up to 12 at health 45 with -2 per reed; player and AI quota/health enforcement occurs before ledger commit, non-reeds and critical needs remain unchanged, canonical events are emitted, and exhausted planning projection is cached
- strict schema-v9 snapshot, run-summary, and artifact persistence with v5-v8 migration/legacy continuation; the minimal HUD shows policy, health, quota, and consequence within the nine-line terminal bound
- V2 artifact exports including world summary and metrics CSV
- opt-in bounded runtime diagnostics and a standalone paired performance runner
- headless .NET + Godot validation coverage
- experimental voxel spike module for chunking, edits, meshing, persistence, and walkability-mask extraction

Deferred or not implemented as authoritative runtime systems yet:

- authoritative multiplayer
- production backend persistence
- voxel terrain as the main gameplay world
- pathfinding-based collision avoidance or crowd simulation
- markets, trade, and broader governance systems beyond the bounded W3-01 policy contract
- W3-04 LLM-readiness/fallback, restoration jobs, general law-system expansion, markets, and broad UI
- combat and social simulation

## Validation Commands

```powershell
dotnet build src/societies/Societies.csproj --configuration Release
dotnet build src/societies/Societies.sln --configuration ExportRelease
dotnet test tests/Societies.Core.Tests/Societies.Core.Tests.csproj --configuration Release
godot --headless --path src/societies res://tests/HeadlessTestRunner.tscn
./scripts/run-prototype-validation.ps1

# Optional Debug characterization; runs matching metrics-off and metrics-on cases.
./scripts/run-performance-pair.ps1 -Scenario balanced_basin -Seed 1337 -Citizens 3 -Ticks 3 -CacheMode cold

# Optional Windows Release-route smoke; requires Godot 4.6.2 .NET export templates.
./scripts/run-performance-pair.ps1 -ReleaseExport -Scenario balanced_basin -Seed 1337 -Citizens 3 -Ticks 3 -CacheMode cold

# Optional three-mode contract; validates cold/warm equivalence plus forced invalidation.
./scripts/run-performance-cache-modes.ps1 -Scenario balanced_basin -Seed 1337 -Citizens 3 -PreconditioningTicks 2 -Ticks 2

# Clean Release comparison of exhaustive and exact branch-and-bound selection.
./scripts/run-job-selection-comparison.ps1 -ReleaseExport -Scenario balanced_basin -Seed 1337 -Citizens 16 -Ticks 300 -Trials 3

# Reproduce current residual spike attribution from the clean W1-05f matrix artifacts.
./scripts/analyze-performance-spikes.ps1 -InputPath artifacts/performance/w105f-baseline-clean-7456072 -OutputPath artifacts/performance/w105f-baseline-clean-7456072/spike-analysis.json

# Canonical W1-03c Windows Release matrix; 14 pairs and 28 metrics rows.
./scripts/run-performance-baseline-matrix.ps1
```

## Validation Notes

Historical W1 profiling bullets below may mention runtime metrics CSV v4 or earlier v5 artifacts; those claims are not current. The current runtime batch contract is `runtime-batch-metrics-v6.csv` with 44 columns.

- W3-01 final local validation is recorded in [v3-w3-01-validation.json](planning/active/evidence/v3-w3-01-validation.json), SHA-256 `b256b8edd2a117cde6253e75c80be638e8c17203ebb2f689dc1d992e66fe7d17`: focused 145/145, full 376/376 .NET with 0 failed/skipped, Godot 4.6.2 headless exit 0 with manifest 23 (console count not independently parsed), and Debug/Release/ExportRelease zero warnings/errors. Deep review is GO with no P0-P3 findings.
- W3-01 performance evidence is [v3-w3-01-performance-validation.json](planning/active/evidence/v3-w3-01-performance-validation.json), SHA-256 `580c98175056cdba1f021d7624434cba70da767ed49fed5a3009654af47ab623`: clean 14/14 pairs and 354/354 hashes; reference median p95 `47.9643 ms` <= 50 and maximum `176.1198 ms` <= 250; soaks `38.5737/205.6055` and `38.0676/192.3137` pass safety/determinism; forced invalidation `22.8738 ms` is correct; c24 `151.4178/198.9147 ms` is characterization-only. The aspirational `target_missed` result is not a safety failure. A general comparative regression assessment was not performed; hard safety did not regress against unchanged thresholds.
- W3-02 final validation is recorded in [v3-w3-02-validation.json](planning/active/evidence/v3-w3-02-validation.json): 410/410 .NET tests, Godot 23/23, Debug/Release/ExportRelease production builds with zero warnings/errors, and deep review GO with no P0-P3 findings. The slice keeps critical Eat/Sleep/recovery behavior unchanged and adds no schema/UI/wetland effects/LLM integration.
- W3-02 performance evidence is [v3-w3-02-performance-validation.json](planning/active/evidence/v3-w3-02-performance-validation.json): clean 14/14 pairs and 354/354 artifact hashes; established milestone median gate is green at reference p95/max `47.4881/178.653 ms`, both soaks and forced invalidation are green and deterministic. The raw runner remains `safety_failure` because one reference trial recorded p95 `63.5804 ms`; formal target remains missed. This is an isolated variance risk with no A-B causal attribution.
- W3-03 final validation is recorded in [v3-w3-03-validation.json](planning/active/evidence/v3-w3-03-validation.json): focused wetland+artifact 37/37, core 129/129, UI 13/13; authoritative wrapper exit 0 in 416.9 s; Debug 0; full Release 442/442 with 0 failed/skipped; Godot 4.6.2 23/23; Release/ExportRelease builds 0 warnings/errors; final staged deep review GO with no P0-P3 findings across 23 files (1,809 insertions/86 deletions).
- W3-03 performance evidence is [v3-w3-03-performance-validation.json](planning/active/evidence/v3-w3-03-performance-validation.json): clean 14/14 pairs and 354/354 hashes; raw matrix status is `target_missed` (not a safety failure). The established milestone gate is green: reference median p95/max `43.1679/213.4861 ms`, soaks `38.8865/164.2170` and `41.3155/175.9617`, forced invalidation `21.9538 ms` correct, and c24 `144.4787/190.5471 ms` characterization-only. W3-02 comparison is non-causal; no same-session A/B was run.
- W2-06 final evidence remains historical milestone context; W2-VIS timing/visual-readback waivers, Demo 1 concept classification, playtest gaps, and historical schema/timing facts remain unchanged.
- The earlier BuildWorkOrders profile remains diagnostic historical context only; it does not override the clean canonical matrix or authorize a separate optimization path.

- The current test suite includes pure simulation tests, persistence tests, HUD tests, voxel-spike tests, and Godot headless smoke coverage.
- W2-06 final validation reports 350/350 .NET tests and 23/23 Godot tests; Release, ExportRelease, and Debug builds pass with 0 warnings/errors on their respective routes; see [v3-w2-06-final-validation.json](planning/active/evidence/v3-w2-06-final-validation.json). W2-VIS remains under its explicit waiver; its timing and visual caveats are unchanged.
- The full validation script is authoritative, but it is substantially longer than the individual .NET or headless passes because it rebuilds and reruns both.
- The optional performance pair is not a pull-request gate. It requires clean committed source by default and writes ignored run artifacts under `artifacts/performance/`.
- Performance result schema v6 records cold, natural-warm, and forced-invalidation cache transitions plus independent non-persistent selector, extraction-planning, and route-distance modes, exact-query accounting, pruning, route reuse, cached-distance replay hits, and phase timing. Current runtime diagnostics use `runtime-batch-metrics-v6.csv` (44 columns). Earlier v4/v5 CSV claims are historical profiling evidence only. Natural warmup advances deterministic simulation state; eager/all-pairs cache prefill remains disabled.
- A single pair can prove only its own transition. `scripts/run-performance-cache-modes.ps1` is the cross-mode authority: it requires identical cold/warm configuration, environment, tick bounds, and deterministic hashes plus a valid forced-invalidation transition.
- The Godot editor/headless execution route loads the managed Debug build, so its metrics-off/on pair remains characterization only.
- The tracked Windows export route uses the `Windows Performance Release` preset and accepts Release evidence only when the exported binary identifies its managed assembly as `ExportRelease`, reports the Godot release/template features, and reports neither a debug build nor the editor feature. It does not silently promote an ordinary managed `Release` build to reference evidence.
- The tracked solution exposes Godot's three managed configurations (`Debug`, `ExportDebug`, and `ExportRelease`) without mapping an ordinary solution `Release` configuration back to Debug.
- Raw catalog JSON is explicitly included in the Windows preset. Editor runs use the validated filesystem directory; exported builds always use packed `res://data` resources, avoiding working-directory-dependent inputs.
- The Release execution route is validated from clean commit `acf634f`; see `planning/active/evidence/v3-w1-03a-release-route-validation.json`. This is route evidence only, not a performance baseline.
- The clean schema-v3 ExportRelease cache-mode comparison passed from implementation commit `5444cc3`; see `planning/active/evidence/v3-w1-03b-cache-mode-validation.json`. It proves cold/warm deterministic equivalence and the forced-invalidation transition for a short three-citizen smoke only.
- The canonical W1-03c ExportRelease matrix passed its evidence contract from clean commit `a636967`: 14/14 pairs, 28 metrics rows, 354/354 manifest artifact hashes, cold/warm equality at 3/6/12/16 citizens, three comparable 16-citizen reference trials, two deterministic 1,000-tick soaks, the 24-citizen stress case, and the forced transition. The budget result is `safety_failure`, not a green performance gate: reference median p95 is 570.6155 ms versus 50 ms safety and median max is 3694.2534 ms versus 250 ms safety. Forced invalidation itself passes at 8.4171 ms. See `planning/active/evidence/v3-w1-03c-performance-baseline-validation.json`. Week 2 feature expansion remains blocked while W1-04/W1-05 correctness and path-selection work continues.
- W1-04 navigation correctness is implemented at `7918d49`: unreachable routes are explicit, blocked endpoints and diagonal corner cutting are rejected, the discounted A* heuristic is admissible, equal-priority ordering is deterministic, cached cell routes rematerialize exact endpoints, and unreachable work is skipped with diagnostics. Existing wetland resources remain usable through deterministic walkable interaction positions, while fresh and restored settlement placements are normalized to walkable cells. Validation passed 110/110 .NET tests, 16/16 Godot headless tests, zero-warning Debug/ExportDebug/ExportRelease builds, and independent review with no P0/P1 findings. See `planning/active/evidence/v3-w1-04-navigation-validation.json`.
- W1-05 exact selection is implemented from `b1fddaf` with clean Release evidence at `227a758`: a guarded straight-line score bound, strict-below-best pruning, ordinal ties, multi-leg reachability checks, and selected first-route reuse preserve exhaustive results. All four shipped scenarios matched per-tick assignments, events, resource application, and final snapshots for 300 ticks. At 16 citizens, selector exact queries fell from 17,441 to 2,544 (85.414%) in every clean Release trial; median p95 fell from 656.3981 ms exhaustive to 78.0301 ms optimized. Validation passed 119/119 .NET tests, 16/16 Godot headless tests, all managed configurations with zero warnings, the optimized cache-mode contract, and independent implementation review with no P0/P1 findings. The post-W1-05 canonical matrix passed all evidence contracts and improved the 16-citizen reference median p95 from 570.6155 ms to 81.4823 ms, but the 50 ms p95 and 250 ms maximum safety limits still fail. Week 2 feature expansion remains blocked while the remaining spikes are isolated. See `planning/active/evidence/v3-w1-05-job-selection-validation.json`.
- W1-05b characterizes those remaining spikes without changing gameplay. A deterministic postprocessor validated all 14 clean schema-v4 ExportRelease equivalence pairs and 5,301 metrics-on diagnostic ticks. Total wall time correlates strongly with path-cache misses (`r=0.982573`) but weakly with lookup volume (`r=0.233264`) and navigation rebuild time (`r=0.076652`). Each 16-citizen reference trial has the same 16 ticks over 50 ms; all seven ticks over 250 ms are initial-cold or immediately post-invalidation, and all six ticks over one second follow invalidation. Both 1,000-tick soaks reproduce the same 50 spike ticks and all 18 one-second spikes follow invalidation. Navigation rebuild itself is small; full-cache repopulation through work-order route ranking and exact citizen selection dominates. The safety gate remains red, so W1-06 and Week 2 remain blocked pending an exact cache-repopulation optimization and a fresh canonical matrix. See `planning/active/evidence/v3-w1-05b-spike-characterization.json`.
- W1-05c materially reduces cache-repopulation cost without changing A-star policy or deterministic gameplay. Exact bounded extraction planning cuts total path lookups by 22.72% to 67.96% across the four 300-tick scenario comparisons. Dense row-major navigation state, pooled generation-stamped A-star workspaces, exact multiplier caching, and shared immutable neighbor topology preserve dictionary-reference routes, float bits, unreachable behavior, ties, versioning, generation wrap, and concurrent queries. Full validation passes 151/151 .NET tests and 16/16 Godot headless tests. The clean schema-v5 14-pair ExportRelease matrix from `a772d15` passes all contracts and 354 artifact hashes: 16-citizen median p95 improves from the immediate pre-slice 81.4823 ms to 36.9614 ms, and median max from 1552.5664 ms to 259.702 ms. The forced metrics-off full tick is 245.699 ms against this slice's 250 ms diagnostic bound, while the formal commit-to-first-correct-lookup target passes at 11.8671 ms. The overall safety gate remains narrowly red: reference max is 9.702 ms above safety, soak maxima are 323.231/322.1246 ms, and 24-citizen stress reaches 272.6378 ms p95 and 607.9651 ms max. Remaining wall time still tracks cache misses (`r=0.959689`), with selector/global-frontier fanout dominating the worst stress tick. W1-06 and Week 2 remain blocked pending a reduction in distinct exact searches and another canonical matrix. See `planning/active/evidence/v3-w1-05c-cache-repopulation-validation.json`.
- W1-05d removes that shared-frontier fanout without using its lower-bound fields as routes or scores. A topology-only geometric field is built only when at least eight distinct current-version route keys are missing, shared by the settlement anchor, central depot, or one exact citizen origin for the current tick, and used only to strengthen strict candidate pruning. The unchanged versioned A-star remains authoritative for every unpruned and selected route. Eight direct lower-bound cases plus four 300-tick exhaustive selector differentials preserve exact worker state, route bytes and float bits, events, resources, endpoints, unreachable behavior, ties, and navigation invalidation; Balanced Basin selector queries fall from 2,544 to 1,482. Full validation passes 159/159 .NET and 16/16 Godot tests, with zero-warning Debug, ExportDebug, and ExportRelease builds and final independent review reporting no P0/P1/P2 findings. The clean schema-v5 14-pair matrix from `8688c27` passes all contracts and 354 hashes. Reference median max falls to 159.8224 ms, both soak maxima fall to 169.5998/165.5849 ms, and 24-citizen stress improves to 128.9719 ms p95 / 165.0386 ms max. Stress tick 292 falls from 67 general + 225 selector misses and 626.3771 ms to 34 + 2 misses and 157.7138 ms. The hard gate nevertheless remains `safety_failure`: one 16-citizen reference trial records 50.9476 ms p95 against the 50 ms limit, despite the 46.9161 ms median p95 and all maximum limits passing. Forced invalidation remains exact and passes at 11.4945 ms. W1-06 and Week 2 remain blocked pending a residual general work-order construction/variance reduction and another clean matrix. See `planning/active/evidence/v3-w1-05d-shared-frontier-validation.json`.
- W1-05e removes cached-route reconstruction from distance-only general work-order queries. A current-version positive general-cache hit now replays the cached cell path's distance with the same endpoint validation and float operation order, without allocating waypoints, copying route cells, or recomputing cost/ticks; misses, negative hits, selector/full-plan queries, and navigation-version invalidation remain unchanged. A full-materialization reference mode plus nine direct bit-exact cases and four 300-tick shipped-scenario differentials preserve routes, distance bits, assignments, events, resources, snapshots, endpoint/unreachable semantics, ties, and invalidation. The measured matrix exercised 316,534 fast-path hits. Full validation passes 174/174 .NET and 16/16 Godot tests, and final independent review reports no P0/P1/P2 findings. Performance/equivalence results advance to schema v6 while runtime metrics CSV remains v4; the spike analyzer accepts both v5 and v6 evidence. The clean 14-pair matrix from `9d5ca14` passes every contract and 354/354 hashes. Reference median p95 is 49.1537 ms and median max is 156.3861 ms; both soaks pass safety at 37.475/37.5293 ms p95 and 170.4334/162.4554 ms max. Stress is 132.4614 ms p95 / 154.5145 ms max, and forced invalidation is exact at 12.7154 ms. The hard gate remains `safety_failure` because reference trial 2 reaches 51.2372 ms p95, 1.2372 ms above safety. This slice reduces repeated cached-path materialization but intentionally does not change the 4,176 general or 1,262 selector exact misses observed across the matrix; distinct-search reduction remains the next blocker. Keep W1-06 and Week 2 blocked. See `planning/active/evidence/v3-w1-05e-cached-distance-validation.json`.
- W1-05f omits an entire extraction resource class before route ranking only when the active unclaimed frontier already contains `K` orders with priorities strictly greater than the class's maximum possible priority. Equality, underfilled frontiers, claimed-ID collisions, exhaustive-reference mode, and uncapped mode fall back to full materialization; the virtual uncapped count preserves diagnostics and final sort/take semantics. Eleven new strict-boundary, call-site, cache, unreachable, and 300-tick differential cases raise full validation to 185/185 .NET plus 16/16 Godot, with zero-warning Debug/ExportDebug/ExportRelease builds and final review reporting no P0/P1/P2 findings. The clean matrix from `7456072` passes 14/14 contracts and 354/354 hashes, reducing total route lookups by 20.157% while preserving the 4,743,965 virtual uncapped orders. All three reference trials pass individually at 45.8724-47.2589 ms p95 and 147.9514-203.0221 ms maximum; confirmatory trials pass at 48.1055-48.6139 ms p95 and 147.7332-206.0087 ms maximum. Both soaks and forced invalidation pass safety, so the hard performance gate is green and W1-06 is unblocked. The aspirational target remains red and 24-citizen stress remains characterization-red at 129.2682 ms p95. Week 2 remains blocked until W1-06 establishes and validates a single resource authority and its snapshot/schema boundary. See `planning/active/evidence/v3-w1-05f-bounded-work-order-frontier-validation.json`.
- W1-06 moves resource quantity into one pure C# ledger owned by `PrototypeRuntimeSession`. Generated sites retain stable legacy-compatible IDs, player and AI harvesting use ordered command/result records, AI failure rollback occurs before the next request, scene nodes are read-only projections, and persistence/metrics/summaries source the ledger. Runtime snapshots and run summaries advance to schema v6 while performance results remain v6, runtime metrics CSV remains v4, and world summaries remain v3. Strict prepare-before-commit restore accepts v5 through deterministic identity migration, requires complete v6 ledgers, derives legacy navigation versions from built paths, and rejects malformed, inconsistent, duplicate, non-finite, unknown, and future state without mutating the live session. Exact 1,000-tick versus save-at-500/resume snapshots and events match. Full validation passes 198/198 .NET and 16/16 Godot with zero build warnings, and final independent review reports no P0/P1/P2 findings. After an initial clean `419ba69` matrix exposed one repeatable 51.3-51.5 ms first-reference p95, exact revision-cached ordered active projections at `8300574` removed repeated per-tick ledger sorting/allocation. The final clean matrix passes 14/14 contracts and 354/354 hashes: all reference trials pass at 45.7490-46.5016 ms p95 and 146.6519-157.3410 ms maximum, both soaks pass at 37.0458-38.0370 ms p95 and 164.5518-165.1034 ms maximum, and forced invalidation is exact at 12.0662 ms. The formal budget is `target_missed`; non-gating 24-citizen stress remains red at 133.8055 ms p95. Every hard Week 1 safety item is green, W1-06 is complete, and the contracted Week 2 crisis slice is unblocked. See `planning/active/evidence/v3-w1-06-session-resource-ledger-validation.json`.
- W2-01 freezes the `empty_stores` crisis contract without taking W2-02 through W2-05 scope. The catalog supplies seed 1701, 12 citizens, two built huts plus a queued third hut, low meals/fuel, reachable berries/logs, an 18-minute deadline, four stability thresholds, both hold durations, and a scenario-only `0.0875` citizen-need rate multiplier; crisis-absent scenarios retain the exact legacy multiplier of `1.0`. A pure `PrototypeCrisisState` owned by `PrototypeRuntimeSession` consumes one authoritative observation per 20 Hz simulation tick; broken conditions reset their holds, paused calls and terminal states do not advance, and fixed-step render cadence does not affect results. Two real no-input `PrototypeRuntimeSession` runs collapse identically at tick 9,735 (8m06.75s), with 7,532 events and trace SHA-256 `e99b79066ae85fc5617ef21295a29ae1dfc4591932ffea127eae7788073daa36`. The candidate-success contract envelope stabilizes at tick 18,384 (15m19.2s) with exact repeat-run hash `46e645177e7a00a74160a5bd5a219448ab137f3d205585b2ae28a54fb786a734`; an end-to-end candidate run awaits the W2-02/W2-03 commands that can produce those observations. Domain state checkpoint/resume is exact and rejects source-impossible hold state. Runtime snapshots intentionally remain schema v6: crisis-scenario save/load fails fast and F6 reports the deferral, while legacy v5/v6 behavior remains unchanged. Contribution commands, directives, terminal events/HUD, and schema-v7 persistence remain deferred. Full validation passes 211/211 .NET and 16/16 Godot with zero managed build warnings. Historical W1-06 performance evidence remains authoritative and unchanged: the hard safety gate is green, the formal target is missed, and 24-citizen stress remains characterization-red. See `planning/active/evidence/v3-w2-01-empty-stores-contract-validation.json`.
- W2-02 adds a catalog-gated `ContributeToStockpile` command plus an ordinal deposit-all batch at the central-depot proximity prompt. Raw `logs`, `stone`, `clay`, `reeds`, and `berries` transfer from player inventory into the existing authoritative depot in one preflighted batch; crafted tools remain personal. Success removes and adds identical quantities once, records deterministic per-resource counters and ordered events, and immediately refreshes the stockpile projection. Empty, invalid-item, invalid-amount, insufficient-quantity, capacity, out-of-range, and duplicate-frame failures leave inventory, depot state, events, and counters unchanged. Citizens consume contributions through unchanged central-depot work orders, so no second resource authority exists. Runtime snapshots remain schema v6: both crisis state and any in-memory contribution counters fail fast until W2-05/schema v7, while untouched legacy v5/v6 round trips remain unchanged. Focused validation passes 15/15 contribution cases, the manifest-owned fast tier passes 133/133, and the real Godot depot input passes within the 17/17 headless suite. The full 226-test soak/coverage route was intentionally not run because Weekly Full Validation owns it. See `planning/active/evidence/v3-w2-02-atomic-contribution-validation.json`.
- W2-03 adds session-owned Neutral, Food & Fuel, and Shelter directives. Constant-time affinity bonuses are included in exact scoring, both selector upper-bound paths, extraction-class omission, and the global work-order frontier; one exact route pass tracks both the neutral and directive winners, so `CurrentOrderReason` claims `Why:` only when the directive changes the winner. Active work is never cancelled, and critical eat/sleep/recovery selection remains dominant. Keys `2` and `3` select directives and refresh the HUD immediately. Schema-v6 capture fails explicitly for non-neutral directive state while the v7 payload shape is frozen for W2-05. Validation passes 14/14 focused directive cases, 20/20 selector cases including 15 three-state 300-tick differentials across all five shipped scenarios, 147/147 manifest-fast tests, 18/18 Godot tests, and zero-warning Debug/ExportDebug/ExportRelease builds. The full 251-test soak/coverage route and a new Release timing matrix were intentionally not run. Final independent review reports no P0-P3 findings. See `planning/active/evidence/v3-w2-03-directive-causality-validation.json`.
- W2-04 domain/session outcomes and the procedural crisis HUD merged through PR #117 at `d519d4d`. V3-W2-VIS remains complete only under its one-time waiver: p95 `55.4529 ms` vs 50 ms, max `142.9547 ms` vs 250 ms, and persistence/hash equivalence unavailable. W2-05 merged through PR #119 at master `f0e88f0` after local validation at `cea7f8a`; its strict schema-v7 persistence contract remains delivered. W2-06 is locally accepted at `478a4d9`: the clean matrix and final validation pass the hard gate, while the aspirational aggregate target remains missed. W3-01 merged through PR #122 at master `7b747af`; W3-02 merged through PR #123 at origin/master `d9e297f`; W3-03 is locally committed at `a513636`; W3-04+ remain inactive pending another Continue V3 decision.
- The voxel spike is experimental only. The authoritative gameplay runtime remains heightfield-based through M3.

## CI Scope

## Snow Globe scheduling comparison (local-only)

The isolated `labs/Societies.SnowGlobe/` research toy now has its first scheduling experiment. The eight-agent scripted control is captured as identical recorded value responses for both shared-snapshot sequential deliberation and controlled parallel deliberation. Each round freezes observations and proposals before the unchanged deterministic ordinal validation/commit surface; committed events replay to identical event and state digests.

Recorded logical latency units, critical-path latency, throughput, and equal dispatch coverage (minimum scheduled turns divided by maximum scheduled turns) are reported deterministically. A deterministic gate fixture proves eight concurrent in-flight requests and deliberately reverse-order completion; the sequential comparison proves one in flight at a time. Missing, mismatched, null, nonpositive-latency, wrong-agent, explicit-failure, and generic-exception responses fail closed without an action commit; invalid latency is accounted as zero. The focused Release build is zero-warning and the lab tests pass 14/14. This remains offline/provider-neutral and outside `src/societies/`, with no Godot, credential, network, provider, or production-runtime integration.

The completed fixed comparison also has a file-I/O-free canonical evaluation report (`snow_globe_scheduling_evaluation/v1`). Stable UTF-8 JSON records fixed scenario identity, both mode digests and metrics, state/event/replay-equivalence verdicts, and explicit `logical_not_wall_clock` timing semantics. Before allocating report bytes it verifies each fixed world/run digest, tick, mode identity, round/counter coherence, and dispatch coverage; advanced worlds, swapped modes, incoherent digests, and coherent non-equivalent results fail closed. The v1 JSON contract is independently frozen by literals and SHA-256 `20bde9bd80da960f27ebb892576924a604128d01f2659bbb50cfded53a64103c`, BOM-free and without a trailing newline. It excludes timestamps, paths, runtime durations, random IDs, secrets, and host data. The current focused lab suite passes 32/32.

The separate reversible parameter experiment is mock-only and evaluates the exact ordered 4/8/16 × every-tick/every-other-tick matrix. Each matched sequential/controlled-parallel cell uses the same recorded fixture, freezes four deliberation rounds, preserves ordinal commits and replay equality, and measures peak in-flight work with a deterministic mock gate: sequential is 1 and controlled parallel is the exact 4/8/16-cell cohort, capped at 16. `snow_globe_parameter_experiment/v1` rejects missing, duplicate, or reordered cells and derives positive matched total logical latency directly from the fixture: sequential critical path equals that total, while parallel critical path is the recorded per-round maximum. It validates those values, throughput, dispatch coverage, rounds, and counters before canonical serialization; incoherent reports fail closed. The fixed v1 scheduling report remains unchanged. The focused lab suite passes 32/32.

The bounded resilience experiment remains offline and lab-only. Its ordered 8- and 16-agent shared-snapshot cells cover inference timeout, malformed response, adapter crash, queue saturation, and conflicting resource claims. Every primary failure gets exactly one deterministic repair and then, only if the repair is rejected, an `Idle` fallback; ordinal agent-ID commit remains the documented conflict policy. `snow_globe_resilience_experiment/v1` now derives and cross-checks every serialized counter and digest against its canonical trace and fixture before bytes are emitted; the canonical v1 hash remains unchanged. It does not modify `src/societies/`, Godot, network, model, provider, or credential boundaries.

Create-new persistence now uses `snow_globe_run_store/v3`; strict bounded v2 reading/reconstruction remains available, but v2 artifacts are never upgraded or opened for participant append. V3 adds one checksum-bound compound `ParticipantEvaluation` record at tick boundaries, scoped 128-entry idempotency reconstruction, and exact participant-command identity. Scheduled ticks are framed in memory and batch-appended only after full validation; managed adapter/cancellation/validation failures preserve the last checkpoint, while observed uncertain I/O poisons the live writer and makes no crash-atomicity claim. Exact record shapes, allowlisted reasons, whole-operation leasing, capacity bounds, terminal LF, and replay-derived receipts reject corruption and ignored text.

The pure `SnowGlobeLocalModelAdapterPreflight` contract accepts only canonical HTTP loopback endpoints for one future shared local server and opens no socket. It freezes bounded request/output/queue/time/VRAM budgets and validates future 8 GB benchmark evidence from bounded canonical metrics-only bytes, deriving sample count/digest, latency, queue, memory, failure, and fallback aggregates. It performs no credential use, redirect, retry, download, model invocation, GPU probe, or quality assessment.

The headless `SnowGlobeObserverShell` now also accepts paused, bounded structured participant commands through the same atomic expected-identity `SnowGlobeWorld` validator. Admitted accepted, stale, and domain-rejected evaluations have stable idempotent receipts; transient admission failures remain retryable. World collections are detached, mutation ownership uses an O(1) revision, and inspection uses 32-event cursor pages plus a cached full-history digest.

`SnowGlobePersistedSession` is the single mutable owner for v3 pause/resume/advance/step/inspect/participant operations. It binds an identity-bearing adapter before artifact creation or reopen, serializes all operations, reconstructs the live world and durable receipt index from the ledger after success or managed failure, permanently fails closed on poison/coherence loss, and supports nonblocking reentrant/concurrent disposal. Mixed participant and scheduled execution, reopen/resume, idempotency, failure recovery, provenance, concurrency, paging, and disposal pass independent deep review. Final local validation passes 239/239 Release tests, a zero-warning/zero-error Release build, `git diff --check`, and FINAL CODE GO with no P0-P3 findings.

The completed offline two-tier cognition slice adds one deep `SnowGlobeTwoTierCognitionModule` behind the existing `ISnowGlobeIdentifiedInferenceAdapter`, with local and premium lanes while the deterministic world remains the sole state authority. Its immutable policy carries an exact content-addressed premium model revision and binds the financial run/journal; it reserves before submission, marks submission unknown before the fake provider, permits at most one premium attempt, retains an unknown reservation without resubmission, then uses explicit local fallback and deterministic `Idle`. Detached receipts record policy, financial run, requested lane, model/revision, and primary-outcome evidence. The in-memory journal and offline fake are the only financial/premium implementations in this slice. Existing v3 recorded-response replay remains provider- and financial-call-free and digest-equal. Focused tests pass 22/22; the full Snow Globe Release suite passes 318/318; the Release build has 0 warnings/0 errors; `git diff --check` is clean; and independent deep review is FINAL CODE GO with no P0-P2 findings. Durable design records are [CONTEXT.md](CONTEXT.md), [ADR 0001](docs/adr/0001-two-tier-cognition.md), and [TWO_TIER_COGNITION.md](labs/Societies.SnowGlobe/TWO_TIER_COGNITION.md). This is offline research infrastructure only: no HTTP, credentials, provider/model/payment call, network, model download, production durability, live-quality, deployment, raw API resale, Stripe/billing/account integration, or run-store schema change occurred.

## Historical predecessor Snow Globe lab provider/Ollama reconciliation

At this predecessor milestone, the isolated Snow Globe lab had only an offline credential/provider preflight contract. A Credential Lease owns and zeroes its transferred secret buffer; a Fixed Provider Profile is immutable and registry-owned; and a Provider Execution Capability is single-use and binds the exact profile, policy, journal, job, BYOK identity, bounds, source identity, clock, and nonce. The fixture itself has no production profile, authenticated HTTP adapter, live credential, parser/status/charge evidence, payment action, or provider call. The later OpenRouter production bridge and bounded paid attempt supersede any lab-wide absence claim and are authoritative in the current section above.

Evidence: 6/6 focused tests, 348/348 full lab tests, Release build 0 warnings/errors, and independent deep-review CODE GO. This proves only the reviewed fixture and lease cleanup behavior, not arbitrary callback non-retention or live provider quality.

An isolated portable Ollama v0.32.14 runtime at `E:\AIModels\OllamaRuntimeRepair\runtime-v0.32.14` was verified from the official asset (SHA-256 `5AE5BCA5F0D297F5E35665E01DB399A69A8EAC3F8FAD89CD9D2531FD495C9457`). Controlled startup discovered the RTX 2070 SUPER via CUDA compute 7.5 with 8 GiB total and 7 GiB available VRAM, using loopback `127.0.0.1:11435` with cloud disabled; the process was stopped afterward. The default PATH Ollama 0.18.2 installation remains unchanged. The frozen qwen3.5:4b local compatibility cell completed and its canonical evidence was accepted; the local-premium comparison below remains offline and does not establish premium live evidence or quality.

Smoke artifact: manifest/full model digest `2A654D98E6FBA55D452B7043684E9B57A947E393BBFFA62485A7AAC05EE4EEFD`; 5 files / 3,389,984,444 bytes; qwen35; 4,659,865,088 parameters; Q4_K_M. One no-retry local call used stream=false, think=false, temperature 0, num_ctx=4096, and num_predict=96. Wall time 51,056 ms; API total 50,958,399,400 ns; load 29,253,514,900 ns; prompt 82 tokens / 21,438,342,000 ns; output 20 tokens / 262,642,000 ns (~76.149 tok/s output metric). Structured output was 63 bytes, thinking 0 bytes, raw text not retained, SHA-256 `B9E223C20EA06E2D48FD96C151B095A8A7494527CD9D6AAD69B24F98FF97D4AD`. All 34/34 layers GPU-offloaded; `/api/ps` size_vram 3,128,038,521 bytes; loaded-state GPU use 6,357/8,192 MiB. No outbound traffic occurred; server stopped/no listeners; model retained on E:.

The frozen qwen3.5:4b compatibility cell completed under a fresh committed-monitor capability. Tags passed 1/1 HTTP 200; warmup passed 1/1; measured requests passed 10/10 with failures=0 and fallbacks=0. Outer wall time was 15.709 s; p50 was 887.0709 ms; p95=p99=max was 1,036.6006 ms; throughput was 57.637718 tok/s. Queue bound/peak was 1/1, total/peak wait was 0.5206 ms, and maximum request/output sizes were 801/873 B. Static VRAM was 6,351 MiB; sampled peak was 6,432/8,192 MiB against a 6,963.2 MiB ceiling across 179 samples. The TCP monitor recorded 482 clean bounded samples and explicitly reports `bounded_samples_do_not_guarantee_unsampled_transient_exposure`; this is not proof between samples. `external_server_startup_configuration_verified=false` remains explicit. Canonical evidence is `artifacts/snowglobe/local-model/qwen3.5-4b-frozen-benchmark-v1.json`, 3,618 B, SHA-256 `961B54B7D8CFB2AEAD566579499ADB3AA21F1D85BFBE0B7C6FC504A8ADC40E0D`. Cleanup found zero model processes/live attributable rows; no raw output was retained. Offline validation accepted the canonical artifact with 0 errors, and the artifact was unchanged during review. Final independent evidence review is FINAL EVIDENCE GO with no P0-P2 findings. This proves local compatibility, fit, and latency for this frozen cell only—not general intelligence, quality, or production readiness.

The offline local-premium comparison adds one deep `LocalPremiumComparison.Evaluate(ReadOnlyMemory<byte>)` Module entry. A registry binds the exact completed local artifact SHA-256 `961B54B7D8CFB2AEAD566579499ADB3AA21F1D85BFBE0B7C6FC504A8ADC40E0D` and frozen plan/workload/prompt/schema/context/output/sample identities. The strict 16 KiB/depth-8 canonical parser reuses `ValidateBenchmarkEvidence`; internal absent/offline fixture premium Adapters cannot count as live. Current status is exactly `insufficient_live_premium_evidence`; `premium`, `premium_cost`, and `performance_delta` are null. The canonical report is 2,015 B, SHA-256 `845c429f3d1f90da13111affb2adf5480e6bbb72aa8a95e04de07730080dadce`; the comparison contract hash is `5ca8f57d8dd4fb5de18a1179c1a8acf25eef944ac7350f30514f097932d95227`. No winner, quality/intelligence result, or local-cost-zero claim is made. The slice performs no file/network/provider/credential/payment/model/journal Apply/world mutation. Focused validation is 7/7, full lab validation 374/374, build 0 warnings/errors, and independent CODE GO has no P0-P2 findings.

The offline Cognition Quality Corpus v1 is implemented in the isolated Snow Globe lab. It contains exactly 12 survival-progression scenarios across four categories, reconstructs legitimate scratch worlds, and keeps `ValidateAndCommit` as feasibility authority. The closed integer rubric has 1,200 maximum raw points across the corpus and exactly five dispositions: `no_proposal`, `contract_invalid`, `domain_rejected`, `feasible_suboptimal`, and `maximum_utility`. Canonical content-addressed corpus, scoring, submission, and report binding is published with corpus digest `4de8c4a993b58875f27c5867c29a54679de789dacb03d2b4d8099e26340f1f8f`, scoring digest `043dc7f01ae544d4698e9c8b44c0f2c27b9f0a66fdba3a1e2249b868a64c35b0`, and all-optimal 1200/1200 report digest `7d7d918caa0f11f2367fabf1cc538c38d014b97c53acd8b32f94acbb0678652c`. Submitted AgentId is strictly bounded to 1..64 ASCII lowercase letters, digits, or hyphen, and the submission envelope is bounded to 16 KiB. Focused validation is 12/12, full SnowGlobe Release validation is 386/386, the Release build has 0 warnings/errors, and independent deep review is FINAL CODE/DOC GO with no P0-P2 findings. This is fixed-corpus single-step utility evidence only: it is not general intelligence, model quality, a provider winner, or price evidence. No live model/provider/credential/payment action occurred, and the corpus has no provider/model/file/network/world authority.

Historical/superseded next action: implement an offline provider-neutral cognition-quality execution-evidence contract that binds an exact model/policy revision and recorded 12-proposal submission to the corpus, scoring, submission, and report digests before any separately authorized live corpus run. The contract is now complete; the later v3 Ollama recording-composition handoff is recorded below.

## Isolated Snow Globe Lab (local-only)

The durable Financial Journal milestone adds the BCL-only `snow_globe_financial_journal/v1` module with a small command/query interface and in-memory plus strict-file adapters. Proposal caller and run-store v3 interfaces are unchanged; the deterministic world remains sole authority. Immutable checksum-bound headers, mandatory checksummed LF JSONL, live-writer lease, strict bounded canonical validation, fail-closed corrupt/torn handling, append-uncertainty poisoning, opaque `byok-account-sha256-<64 lowercase hex>` binding, reserve/dispatch-unknown order, one reserved reopen dispatch, Unknown retention, idempotency/conflict, cap/reconciliation evidence, and immutable receipts are validated. Four-record premium-admission and two-record persisted-denial headroom are protected; at absolute capacity the module returns cached deterministic `Idle` without provider/local calls and intentionally cannot create a durable idempotency key. This proves ordinary restart recovery for successfully flushed records under one host/one writer only; it is not power-loss certified, multi-process/multi-host/account-wide transactional, commercial accounting, cross-ledger atomic, or exactly-once charging. SQLite/transactional DB remains required for those claims. No HTTP/auth credential/payment/model/network/provider call, live BYOK secret, payment account, `src/societies/`, or run-store schema change occurred.

labs/Societies.SnowGlobe/ remains a separate headless .NET 8 research toy, not part of the authoritative Godot runtime under src/societies/. It includes deterministic scheduling/resilience experiments, strict v3 participant-aware persistence with read-only v2 compatibility, transactional observer controls, a durable participant session, and a production-capable Ollama benchmark boundary. The approved qwen3.5:4b artifact is retained in the E: model store after the completed frozen compatibility cell. The offline local-premium comparison has insufficient live premium evidence and makes no winner or quality claim. The pinned runtime remains loopback-only and the default PATH Ollama 0.18.2 installation remains unchanged.

Historical pre-repair checkpoint: controlled Ollama 0.18.2 on isolated `127.0.0.1:11435` with cloud disabled and one parallel slot reported GPU bootstrap `initial_count=0` and `total_vram=0`, including when forced to `cuda_v13` and the exact GPU UUID. `nvidia-smi` independently confirmed the supported RTX 2070 SUPER (CC 7.5), driver 581.42, CUDA 13.0, and 8192 MiB. The installed Ollama directory had only 7 files/69,826,339 bytes and no GPU libraries. No model pull, model/provider inference, credentials, simulation/code/tests, `src/societies/`, or live-quality claim had occurred at that checkpoint; controlled processes were stopped and port 11435 was closed.

Historical repair handoff: the official v0.32.14 updater was relocated to `E:\AIModels\OllamaRuntimeRepair\updater\OllamaSetup-v0.32.14.exe` (1,564,916,544 bytes, SHA-256 `63061ab02eab0644ec8db56807d8f3e79be19ade9e7c5839014bfc01fd6f1a01`, same valid Ollama Inc. signature/version). Six attributable temporary artifacts totaling 247,012 bytes were moved under the repair tree; no unrelated data was moved. Deep review corrected unsafe inherited/explicit ACLs. That pre-repair handoff is superseded by the qwen3.5:4b smoke evidence above; the default PATH install remains unchanged and the production benchmark runner remains uninvoked.

- GitHub `master` protection requires an up-to-date `build-test-smoke` check on a pull request, applies to administrators, requires resolved review conversations, and blocks force-pushes and branch deletion.
- Markdown-only pull requests run `git diff --check` and skip the managed/Godot toolchain.
- Other pull requests run the Release build and current manifest-owned fast/Godot tiers; superseded runs on the same pull request are cancelled.
- `Weekly Full Validation` runs Friday evening in Vancouver and on manual dispatch. It owns current manifest-declared required .NET/Godot coverage and the extended test project; verify counts from `tests/test-manifest.json`.
- Release-route performance matrices and sprint-end evidence remain explicit milestone validation rather than per-pull-request checks.

## Snow Globe Lab cognition-quality execution-evidence handoff

The isolated `labs/Societies.SnowGlobe/` lab now has the offline `snow_globe_cognition_quality_execution_evidence/v1` contract. Its single pure synchronous `Create(provenance, exact ordered 12 submissions)` operation snapshots the ordered batch once, evaluates the existing frozen corpus scorer, and emits standalone canonical evidence embedding the recorded submission and quality report. The envelope binds provenance, corpus, scoring, submission, report, payload, and final evidence digests and is bounded to 64 KiB.

Local provenance binds canonical model identity, a SHA-256 model revision, exact execution-policy/contract digest, prompt revision, proposal schema, and adapter identity. Premium provenance derives and binds the execution-policy digest plus model, prompt, and schema identities from one validated `ModelPolicySnapshot`; it does not retain the snapshot object or emit raw host, route, or cost fields. Provenance is caller-attested identity, not execution attestation. The contract has no provider, network, credential, payment, journal, file, live action, or world authority and makes no general-intelligence, model-quality, winner, or cost claim.

Published evidence includes local provenance `8fb647f1f9e8a515ad490ccaec1372c4d2c110efa5599c33f93bf087a8821cfc`, submission `9473f2021caffd85586d32ea550f46ee717d082b6d1dcba50ab979c8832a2757`, report `7d7d918caa0f11f2367fabf1cc538c38d014b97c53acd8b32f94acbb0678652c`, local payload `353c266be57dce3b4e3f15bc67920ac3325df75cae0eca00529ffa348014b9dd`, and local evidence `7130deb0945697a14631ddea9bdc29e699b4b1217ae948a729e39ec827f3272a`; premium provenance `e5fb33c1246784b3ff70165ea531297b2fe069d6425d002a770db62b82f32540`, payload `551a06855ee776ea03e8d27e1546806b9308a0517fbe1861e4d3180908ec9261`, and evidence `5a4efa252c84feadfb5ce878e0b6d50f2ceef6f51f2818667865475db666408c`.

Validation: focused evidence tests 7/7, full SnowGlobe Release 393/393, Release build 0 warnings/errors, and deep review GO. This slice remained offline; no live model/provider call, download, network, credential, payment, or `src/societies/` change occurred. See [the execution-evidence contract](labs/Societies.SnowGlobe/COGNITION_QUALITY_EXECUTION_EVIDENCE.md) and [ADR 0005](docs/adr/0005-offline-cognition-execution-evidence.md).

Historical/superseded next action: implement an offline no-network recorded-response corpus runner contract that converts 12 bounded response fixtures into the exact proposal batch under pinned prompt/schema/model identity. That runner is complete in `c7926d3`; current truth follows.

## Snow Globe Lab cognition-quality recorded-response runner handoff

The isolated lab now includes `snow_globe_cognition_quality_recorded_response_run/v1`: one pure synchronous operation over exactly 12 ordered, already-recorded response-byte fixtures. Each fixture must match the frozen scenario ID and observation digest. Responses are bounded to 1..1,024 raw bytes each and 12,288 bytes aggregate; invalid UTF-8 maps to `response_utf8_invalid`, and canonical output is bounded to 96 KiB. Envelope corruption aborts the whole operation. Correctly bound malformed or semantically unparseable content maps to a null proposal and `no_proposal` with a closed parse outcome, while representable wrong-agent or action-specific invalid proposals remain typed input for the existing scorer and deterministic `ValidateAndCommit` authority.

The output is detached and retains no raw response bytes. It binds separate runner, parser, and proposal-schema identities, the caller-supplied prompt revision, caller-attested provenance, ordered response byte counts/digests/outcomes, and nested execution evidence. Caller attestation is not execution attestation. This runner is distinct from v3 normalized recorded-response replay: it does not replay or commit into authoritative or persisted simulation state. The existing scorer reconstructs a private scratch world and uses `ValidateAndCommit` only as feasibility authority. The runner has no network, model, provider, credential, payment, journal, file, authoritative-world, or live-call authority and makes no quality, intelligence, winner, or cost claim.

Code and tests are committed locally at `c7926d3`. Validation passed 11/11 focused runner tests, 404/404 full Snow Globe Release tests, and a Release build with 0 warnings/errors; independent deep review returned GO. Preferred digests are payload `61cacfd4ad26512c1100a9235ee0ab534ec5945527a4da9252486ebf26675e43`, canonical run `f03577c7d6d34f18c8a6c25c61bb3f1ac8f5d0a90ab3c1c208745fd11cb61ffd`, and nested execution evidence `2700886cef55abea3aba76f0789993cd6ad7283fa22d303dac5bbbd302e1ffe8`. See [the runner contract](labs/Societies.SnowGlobe/COGNITION_QUALITY_RECORDED_RESPONSE_RUNNER.md) and [ADR 0006](docs/adr/0006-offline-cognition-quality-recorded-response-runner.md). No push, PR, live model/provider call, credential/payment action, or `src/societies/` change occurred.

Historical/superseded next action: implement an entirely offline canonical prompt-envelope builder for the same 12 frozen observations. Completed in `bcba42a`; current truth follows.

## Snow Globe Lab cognition-quality prompt-envelope handoff

The prompt-envelope action is complete in code commit `bcba42a`. `CognitionQualityPromptEnvelopeBuilderModule.Create` publishes `snow_globe_cognition_quality_prompt_envelope_publication/v1`: exactly 12 corpus-ordered compact UTF-8 prompts under `snow_globe_cognition_quality_prompt/v1`, each embedding one canonical caller-supplied prompt revision. Each containing slot binds its scenario ID and observation SHA-256 and publishes prompt byte count/digest/base64 bytes plus empty response fields. Prompts include survival order, costs, rules, observation, and strict response grammar, while excluding scenario/category, score, preferred answer, setup/state/event, model/provider, credential, and financial data.

Prompt limits are 1..2,048 bytes each, 24,576 aggregate, and 64 KiB for the publication. The canonical publication binds corpus/scoring/validator plus runner/parser/proposal identities, prompt bytes/digests, prompt-set/payload/final digests, and claim limitations. The publication is raw-response-free; `BindRecordedResponses` validates exact provenance prompt revision/proposal schema/count and returns detached fixtures for the existing runner, which necessarily retains detached response bytes until consumed. Caller attestation is not transport-delivery or execution attestation. No model/provider/network/credential/payment/journal/file/authoritative-world action occurred and no quality/intelligence/winner/price claim is made.

Code and tests are committed locally at `bcba42a`. Validation passed 6/6 focused prompt-envelope tests, 410/410 full Snow Globe Release tests, and a Release build with 0 warnings/errors; independent deep review returned FINAL CODE GO. Preferred digests are payload `d879faa5af02e5b95108d7b9355a763acee1e120a1c68986c62c0e3b8907ce87`, canonical `966727433db3095e804148bba18e23da368d5fbbf58e7b0e2e58de349b47e9ae`, and prompt set `f9baf35ff43fbd4977d050488f0bb1ebfb37bb9b1fb98ddbd2fa83384e9bbcbb`. See [the prompt-envelope contract](labs/Societies.SnowGlobe/COGNITION_QUALITY_PROMPT_ENVELOPE.md) and [ADR 0007](docs/adr/0007-offline-cognition-quality-prompt-envelope.md).

Historical/completed next action: implement an entirely offline recording-evidence envelope that atomically binds this prompt publication and prompt-set digest, provenance, exact ordered response digests, and existing runner evidence. Completed in `bf756ed`; current truth follows.

## Snow Globe Lab cognition-quality recording-evidence handoff

The isolated lab now includes `snow_globe_cognition_quality_recording_evidence/v1` with semantics `offline_recording_evidence_binding_only`. Code commit `bf756ed` adds one pure synchronous `CognitionQualityRecordingEvidenceModule.Create(publication, provenance, exact ordered responses)` operation. It validates and embeds the exact existing prompt publication, caller-attested provenance, exact existing recorded-response run, and nested execution evidence, while adding an ordered response-set digest over scenario ID, observation digest, response byte count, and response digest. There is no Adapter or port at this in-process seam.

The operation requires exactly 12 responses of 1..1,024 bytes, with a 12,288-byte aggregate limit and a 192 KiB final canonical-artifact limit. All-or-error promises only in-memory atomicity. The raw-free result retains no response bytes; Module-owned temporary snapshots and detached fixtures are cleared, while caller-owned input bytes are never cleared. Correctly bound malformed content remains the closed runner outcome `no_proposal`; publication, provenance, envelope, or coherence corruption aborts.

Response association and identity remain caller-attested. The envelope does not prove prompt delivery or model execution and contains no provider status, retry, or charge evidence. It makes no model-quality, general-intelligence, winner, or cost claim and grants no network, provider, credential, payment, journal, file, authoritative-world, or live-action authority. `src/societies/` is unchanged.

Goldens are response-set `0c9ce26bf5f078e3cdcb85a2115f59f9a3e8d191736e8ab8e87c0c113b67e80c`, payload `069aa258c0a6870aa6d8c60f14aed800cbb46923564d3b62f36a41ba3159a7fd`, and final `61d0f7150b4b1cde5fba3f693e1a60eec6410deb83b6a371b62189f59a2115a4`. Focused new-plus-predecessor validation passed 30/30, the full Snow Globe Release suite passed 416/416, and the Release build passed with 0 warnings/errors. Independent deep review returned FINAL CODE GO after four adversarial identity/digest fixes: split-brain publication, false serialized digest, provenance canonical mismatch, and undefined lane-99 regressions. See [the recording-evidence contract](labs/Societies.SnowGlobe/COGNITION_QUALITY_RECORDING_EVIDENCE.md) and [ADR 0008](docs/adr/0008-offline-cognition-quality-recording-evidence.md).

## Snow Globe Lab cognition-quality recording-session handoff

Commit `8256512` historically implemented the offline `CognitionQualityRecordingSessionModule`. The public instance API was `Authorize` and `RecordOnceAsync`; `OfflineFixedResponseCognitionQualityRecordingAdapter` was the sole public runtime Adapter at that commit. Current offline fixture inventory is the generic fixed-response fixture plus registry-closed `OfflinePinnedOllamaRecordingFixture`; neither is live. Exact process-local authorization bound publication/prompt-set/provenance/Adapter identities and contract digest, canonical nonce, capability lifetime, and session timeout. Nonce tombstones were bounded and non-evicting at 1,024 and fixed-response capability tracking was capped at 4,096. Atomic one-use capability enforcement applied to every call, including pre-cancel, expiry, wrong-module, and binding failure.

The session runs exactly twelve canonical slots sequentially, one attempt each, with no retry/fallback/alternate/thirteenth call. Responses are 1..1,024 bytes each and 12,288 bytes aggregate with exact binding echoes; evidence is created only after all twelve. Failure paths are raw-free/no-evidence. Explicitly disposed fake fixtures and prompt copies are zeroed while caller inputs are preserved. Results claim offline fixture only, with no delivery/model-execution authority. No Ollama/premium/network/provider/credential/payment/file/journal/world/live authority is implemented. Process-local authorization is not restart-durable; future Adapters require separate registry/source change and deep security review.

Source/test hashes are `3354932C1BF170C495A59CCA607A8F84F77D64C9E724E9B17AE92B96502190B1` and `BCFFEE0DF340AC1E98C0C089B58F1BDB7A04F4B66F56356BA18CCC0DB400214D`. Focused validation 17/17; full Snow Globe Release 433/433; Release 0 warnings/errors; independent deep review FINAL CODE GO after five corrections. `src/societies/` unchanged. See [the recording-session contract](labs/Societies.SnowGlobe/COGNITION_QUALITY_RECORDING_SESSION.md) and [ADR 0009](docs/adr/0009-offline-cognition-quality-recording-session.md).

The recording-session action is complete/historical. Its former conformance-harness action is now superseded by the completed `8a5d339` milestone below.

## Snow Globe Lab offline recording Adapter conformance handoff

Commit `8a5d339` adds the test-only `CognitionQualityRecordingAdapterConformanceHarness` and its five-test fixture suite. The reusable harness runs through the real public recording-session Module, binds the exact candidate identity SHA-256 digest, Adapter contract digest, expected evidence canonical digest, and ordered checks/results, and returns a bounded raw-free report. It enforces the exact twelve-slot sequential one-shot path: no retry, fallback, alternate, or thirteenth call. Snapshot requests and retained fixture buffers are disposed and zeroed while caller inputs remain intact. The harness performs no I/O, network, live/provider/model call, credential, payment, Ollama, journal, file, or world-authority action and does not alter production live/provider authority.

The fixed `OfflineFixedResponseCognitionQualityRecordingAdapter` fixture is core-conformant but not fully conformant: ten checks pass and `midflight_cancellation` is `not_exercised_by_fixed_fixture`. The async fixture exercises that optional midflight seam and reaches full conformance. This evidence does not certify future provider Adapters, hidden retries, copied buffers outside the exercised path, or security. Source hashes are harness `1DF7E16ABA14AEAEC7B7397A2561A5158180C462A3815DC56146037A177FB23F` and tests `D1CDBCA028781EE192A9697E0FA80FDC620300243A0B0DEEE83F77D5AE8E22FD`; focused conformance validation 5/5; full Snow Globe Release 438/438; Release build 0 warnings/errors; independent deep review CODE GO. See [the conformance contract](labs/Societies.SnowGlobe/COGNITION_QUALITY_RECORDING_ADAPTER_CONFORMANCE.md) and [ADR 0010](docs/adr/0010-offline-cognition-quality-recording-adapter-conformance.md).

The pinned fixture action is complete and historical at `8f95875`. `OfflinePinnedOllamaRecordingFixture` is registry-closed and entirely in-memory; its v2 constructor accepts exactly 12 full Ollama generate wrapper UTF-8 byte sequences, not raw proposal response buffers, and retains frozen qwen3.5:4b metadata bound by a contract digest. The candidate-neutral harness proves its exact eleven ordered checks and full offline result; `OfflinePinnedOllamaRecordingFixtureTests` prove exact count/index read-once behavior, fixture-owned response/request zeroing, caller preservation, concurrent capability safety, and cancellation. Generic session timeout semantics are predecessor evidence, not pinned-fixture timeout evidence. No path/PID/file/env/socket/process/provider/model/credential/payment/live authority exists; transport and model execution are not attested. Validation is 12/12 focused, 445/445 full Release, 0 warnings/errors, and 56/56 benchmark CLI before the final narrow correction; deep review is CODE GO. Hashes: fixture `B308723D8253222458BAD80A6B14041178CDED8829DC6C1B276F156EF91FA0B3`, harness `4AF2C88F5199ACAD1E9CA2EB78DB9877E145C1DDEA239797DE09D0085F7E7C3A`, tests `C96C92B01294C37444612C859D2B3D0F562E9DE1867318BE4F6809A4F7D1D0EC`.

The former codec/fake-port action is historical and complete in `016551c`; see [the offline Ollama recording codec contract](labs/Societies.SnowGlobe/OFFLINE_OLLAMA_RECORDING_CODEC.md) and [ADR 0012](docs/adr/0012-offline-ollama-recording-codec.md). It remains offline evidence only: no current Ollama behavior, delivery, or model-execution claim is made.

The former loopback transport and v1 composition attempts are historical. The pre-attempt v2 implementation from `a5a0823` and attempt-003 from `6e47510` are historical completed evidence; the v3 section below is historical/superseded. V1 tombstones remain untouched. No accepted body/wrapper/nested evidence/12-slot/quality/compatibility claim is carried forward.

Attempt-003 is historical EVIDENCE GO: one preflight, one record, one validate, and one HTTP200 POST. TransferEncoding was typed code-path evidence, not independent wire capture. The historical v3 section below records its superseded offline framing action.

## Historical Snow Globe v3 live settlement (Attempt-004; completed action)

Clean HEAD `18f2dc622ce27f14dd9f5d4126176a944244ae8d` is FINAL EVIDENCE GO with no P0-P2 findings. Static preflight was accepted after correctly scoping the GPU gate to non-Ollama WDDM apps. Server `server1`; CLI preflight 1 succeeded with plan `13788130a3573ba8205cf833495e877ca26fb0daecab421bcab27880d4cb4e31`; record 1 succeeded in 28,609 ms; validate 1 succeeded in 148 ms. Exactly 12 ordered `POST /api/generate` requests returned 200 in 20.2868581 s; no retry/fallback/alternate/pull/update/cloud/credential/payment action occurred.

Exact identities are plan `snow_globe_ollama_recording_composition_plan/v3`, receipt `snow_globe_ollama_loopback_recording_receipt/v3`, artifact `snow_globe_ollama_recording_execution_artifact/v3`, and transport `snow-globe-ollama-loopback-recording-transport-adapter/v3`; path `artifacts/snowglobe/local-model/qwen3.5-4b-recording-execution-v3.json`. Artifact is 9,621 B, SHA/canonical `12c3e0f9b8fe13f8eaf2525642e130e4298e18c37f2fff58c2a316d2292f7b67`, payload `da3797cfe041ec949083eaf6e5ec9fecd22df4564ff767b176d94e2da10a50a1`; receipt is 6,169 B, SHA `e1a43bc8b7c44dfde6d71e372f1c6237239efe4ffd60716b869be72bf9dcb6b1`, payload `4d3541fff307a3f7dcd5aea1958c51ba0cc49f7b62df01ee07b086868dfb97fc`; nested digest `cd846a45a85085d1943ce8eb0c8b10ad489a802c1727f58d9d1ca04328e594e7`.

All 12 slots completed with `ResponseReceived`/200, `NotApplicable`, checkpoint/policy `None`, zero counters, and `additional=false`; validator accepted. CUDA RTX2070S was 34/34. Operator cleanup was zero but not independently retained or re-observed; raw-free/cloud/body-log were false. Limits: HttpClient-exposed framing only, no raw-wire proof, nested scoring digest not embedded/revalidated, retained captures cannot prove absence of overwritten invocations, HEAD is provenance not artifact field, and cleanup is operator observation. No quality/intelligence/winner/cost/commercial/general-compatibility/world-authority claim. The v3 action and its score-summary next action are complete and historical.

### bd89187 composition milestone (historical as-of bd89187)

`bd89187` is locally committed, not pushed, in a PR, or deployed. It changes exactly 11 files: `labs/Societies.SnowGlobe.RecordingCli/AssemblyInfo.cs`, `labs/Societies.SnowGlobe.RecordingCli/Program.cs`, `labs/Societies.SnowGlobe.RecordingCli/Societies.SnowGlobe.RecordingCli.csproj`, `labs/Societies.SnowGlobe/OllamaRecordingArtifactStore.cs`, `labs/Societies.SnowGlobe/OllamaRecordingComposition.cs`, `labs/Societies.SnowGlobe/OllamaRecordingExecutionArtifact.cs`, `tests/Societies.SnowGlobe.RecordingCli.Tests/OllamaRecordingCliTests.cs`, `tests/Societies.SnowGlobe.RecordingCli.Tests/Societies.SnowGlobe.RecordingCli.Tests.csproj`, `tests/Societies.SnowGlobe.Tests/OllamaRecordingArtifactStoreTests.cs`, `tests/Societies.SnowGlobe.Tests/OllamaRecordingCompositionTests.cs`, and `tests/Societies.SnowGlobe.Tests/OllamaRecordingExecutionArtifactTests.cs`.

The fixed artifact is `artifacts/snowglobe/local-model/qwen3.5-4b-recording-execution-v1.json`; schema is `snow_globe_ollama_recording_execution_artifact/v1`, maximum 128 KiB. The Module has only zero-I/O `Prepare`, atomic one-shot `ExecuteAndPublishOnceAsync`, and bounded `ValidateArtifact`. It reserves a safe CreateNew target before inner authorization/recording, writes raw-free terminal/success evidence, and leaves an indeterminate tombstone on writer uncertainty without delete/retry. Windows store verification uses the same no-follow pinned single-link handle for final read, blocking ancestor/final swaps, reparse points, hardlinks, and outside-root reads. The artifact is strict canonical/digest validated and embeds raw-free receipt data plus nested evidence digests only.

Validation for the pre-be7c691 milestone was focused 46/46, full SnowGlobe 575/575, RecordingCli 8/8, BenchmarkCli 56/56, lab/CLI builds 0 warnings/errors; independent deep review FINAL CODE GO, no P0-P2. Earlier transient benchmark theory was isolated 5/5 and final full green. No live Ollama/listener/socket/HTTP/process inspection/file hash/model/GPU/provider/credential/payment action occurred in that pre-be7c691 milestone; actual Windows/store/live execution was then uninvoked. This is historical offline code proof only. See [the composition contract](labs/Societies.SnowGlobe/OLLAMA_RECORDING_COMPOSITION.md) and [ADR 0014](docs/adr/0014-offline-ollama-recording-composition.md).
