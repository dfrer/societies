# OpenRouter premium evidence boundary

This is the current production one-shot OpenRouter contract. It extends the reviewed offline boundary with a Windows-only production bridge and CLI, authenticated metadata preflight, durable single-writer journal, sealed bounded authorization, and a fixed raw-free evidence path. It does not authorize retries, alternate providers, hidden fallback, world mutation, deployment, or an unbounded paid run.

## Frozen boundary

The selected model API id is `openai/gpt-5.6-luna`; the authenticated canonical release is `openai/gpt-5.6-luna-20260709`. The selected endpoint is Azure with ZDR required. The paid request uses strict structured JSON, `stream=false`, provider `order` and `only` pinned to Azure, no fallback, data-collection denial, ZDR, price sorting, and hard prompt/completion price ceilings. Preflight performs exactly three authenticated metadata GETs and no paid inference.

Each paid authority is single-use and permits at most 12 sequential requests, 4,096 input tokens and 512 output tokens per slot, 8 KiB request/response bodies, a 15-second credential lease, a 240-second aggregate window, and an 18,000-microusd (`$0.018`) aggregate reservation. The request uses minimal reasoning with reasoning excluded from the response and requires authenticated model/ZDR support for that parameter; only finish reason `stop` is admitted. The 1,500-microusd per-slot reservation covers the 1,434-microusd worst-case token exposure at pinned prices. The window covers twelve full leases plus 60 seconds of overhead and may never outlive the authenticated key attestation. Redirects, retries, fallbacks, alternates, proxies, cookies, ambient authentication, and decompression are disabled. Post-dispatch uncertainty remains `SubmissionUnknown` / `ChargeState.Unknown`; local zero settlement is never a zero-cost claim.

The OpenRouter secret is stored in Windows Credential Manager under the fixed application target and is never accepted through argv, environment variables, configuration files, or retained evidence. Documentation records no secret value. Cleanup claims cover application-owned mutable buffers; immutable framework-owned Bearer copies cannot be globally proven erased.

## Standing completion authority and current diagnostic

Standing user authorization permits the successive bounded generations needed to reach an accepted OpenRouter proposal. It does not create retries or an unbounded run: each generation independently preserves Azure-only/ZDR-required routing, at most twelve sequential one-attempt slots, no fallback/alternate, the 18,000-microusd maximum reservation, and one invocation each of plan, preflight, paid `record-once`, and validate. A terminal or uncertain outcome stops that generation; consumed stages never rerun; corrections are reviewed and merged before the next generation.

The merged finite diagnostic classifies a strictly validated HTTP-200 error object by its already-required integer `error.code` only. The closed outcomes cover the official chat/API families 400, 401, 402, 403, 404, 408, 413, 422, 429, 500, 502, 503 plus the official SDK's edge-timeout 524 and provider-overload 529 families. Every other accepted integer remains historical `provider_error_terminal`. The classifier never incorporates the provider message, metadata, raw body, dynamic property, or integer text. It does not reinterpret the numeric family as proof of provider message, transport status, charge, or submission state. See the official [Errors and Debugging](https://openrouter.ai/docs/api_reference/errors-and-debugging), [SDK error list](https://openrouter.ai/docs/agent-sdk/typescript/api-reference/chat), and [Router Metadata error-envelope guidance](https://openrouter.ai/docs/guides/features/router-metadata).

Red-first fixtures failed all fourteen new finite expectations while three unknown-code controls passed against the old generic result; the identical loop passes 17/17 after correction. Focused parser/history/vocabulary passed 118/118, OpenRouter coverage 254/254, full Snow Globe 1070/1070, OpenRouter CLI/security 100/100, Recording CLI 59/59, both Release builds 0 warnings/errors, and independent deep review was FINAL GO. PR #156 passed its required check in 4m22s and merged as `cb3aac0`.

From that exact merge, plan/preflight/paid `record-once`/validate each ran once under the frozen contract. Authorization `796151a2c4f95071d0577aebcc6e7c0c68fafa4dcd6fd035f078274f8ad7f39b`; generation `g2-73e72cc01b5140e1ff1709d647e8491061fde5193a090e6dedc8fafd7fab1e61`; preflight artifact 2,344 bytes/SHA-256 `4c46219057fc28b2785cb1ee8987db4b21ea00d52c353d0be2701ef5d070e830`. One Azure/ZDR exchange stopped `provider_response_rejected_response_usage_completion_tokens_details_unknown_property`; no second slot, retry, fallback, alternate, or proposal occurred. Evidence is 2,287 bytes/SHA-256 `4f98fd4ffa8028a7bf8aafb02815e7adb55db5d7cb5ff831728dd511e35d48ad`; receipt is 793 bytes/SHA-256 `1a6fd3891ecfa4ff7cb0e94d518dc71ecfd6928843b2b71ab13bca57d7f75de4`; journal is 2,891 bytes/SHA-256 `f9f1059ff435aafd5a39122c1933033975ed5ddd952135aaa08021a039477562`, four records, final checksum `f30b4cafe21c978ec578d877b36987050ecbda65002950a69f3880f9ae600cef`. Submission and charge are Unknown, trusted token/local-settlement counters are zero, proposal is null, response digest is nonzero, and the generation/stages are consumed.

The current offline correction makes only `usage.completion_tokens_details` name-additive. Every value must remain a JSON integer from zero through the fixed maximum output-token bound; none is retained or trusted. All other strict scopes, known accounting, cost ceilings, routing/Azure/ZDR, proposal schema, finish/refusal gates, secret handling, and no-retry behavior remain unchanged. Red-first failed only the intended valid additive case while nine malformed/boundary/unchanged-scope cases passed; the identical loop is green 10/10. Focused parser/history/vocabulary passes 127/127, OpenRouter 263/263, full Snow Globe 1079/1079, OpenRouter CLI/security 100/100, Recording CLI 59/59, builds 0/0, and independent deep review is FINAL GO with no P0-P3. No provider, credential, account, retained-artifact, or `src/societies/` action used this correction. See official [Usage Accounting](https://openrouter.ai/docs/cookbook/administration/usage-accounting). Its next use is one governed generation from the reviewed merge.

## Historical post-correction generation terminal evidence

Fresh authority was reviewed and merged through PR #154 as `31a284b`. From that exact source, Release rebuilt with 0 warnings/errors, and the zero-I/O plan, authenticated preflight, paid `record-once`, and local validate each ran exactly once under the frozen Azure-only/ZDR-required, twelve-slot, no-retry/fallback/alternate, 18,000-microusd maximum-reservation contract. Plan digest was `eea26a92d318a2ba102c7979d0cb44563d8bef967ae00b627bc6263ff59d759d`; preflight accepted authorization `b73c2447a22046d36123b7615832697557567c4e1dd74c75ae37587414660fb8`, 2,344-byte artifact SHA-256 `ea7a19664af8039bc3b827a775293d0b0c6ec19b2403e2f500c59fec77dd52e0`, and generation `g2-28a74bf64ac12fc54ff6b5ad9e1597c98a17fe3d04c74abce2c239f734a8998b`.

The paid stage made exactly one Azure-only ZDR exchange and stopped `provider_error_terminal`; no second slot, retry, fallback, alternate, or accepted proposal occurred. Local validation accepted the terminal evidence. Evidence is 2,165 bytes/SHA-256 `b5b02bf6ff3be1e00dd7001f8159dddeb26c60fc543a3eb43d6a1083e45a9b69`; receipt is 793 bytes/SHA-256 `ad2bfc8a8f59b0c135f0b760770f4b537ddfd91d68cbd3665a03770908f3a73e`; journal is 2,830 bytes/SHA-256 `a279337d3edc2b4e2f71d1fc13d225bdc067bea44a7e10a3ef8cea08af3c88c6`, four records, final checksum `cbf980f83f953661d32ff4731e509522517b67537deebc376aed800a7c406e85`. Submission and charge are Unknown, trusted token/local-settlement counters are zero, proposal is null, response digest is nonzero, and both consumed claims exist.

The deterministic classifier establishes only that an HTTP 200 response contained an error object that passed the strict local error schema. It does not establish or retain the provider error code, message, metadata, response body, prompt, refusal text, exception detail, or secret. Independent deep review is FINAL EVIDENCE GO with no P0-P3 findings after independently verifying the documented whole-file hashes, evidence payload digest, journal checksum chain, execution/validation claim payload digests and bindings, one-exchange boundary, documentation, and absence of secret-like diff material; it did not access the trust-anchor credential or reverify claim-envelope HMAC authenticity. This authority and all four stages are consumed; no rerun or second generation is authorized. The terminal family provides no evidence for another local parser correction, so accepted OpenRouter compatibility remains unproven.

## Historical provider-completion generation 4: prompt-detail terminal evidence

Location-diagnostic PR #151 passed required `build-test-smoke` in 4m31s and merged as `63288d2`. From that exact source, Release rebuilt with 0 warnings/errors, and the zero-I/O plan, authenticated preflight, paid `record-once`, and local validate each ran exactly once. Preflight accepted authorization `ae157e559ce8c95a9ac5ff331766f607565aae5144b74489f6266a031e5dd9fc`, 2,344-byte artifact SHA-256 `468e26bb5dc0ccb5aa2d401d9bcab746c04333fe093f0c7b77bd0c81a5eca914`, and generation `g2-bcec3e99ac2a434927a118ff446a8fce0b0e1f81bb66190b118550da9322b700`.

The paid stage made one Azure-only ZDR exchange and stopped `provider_response_rejected_response_usage_prompt_tokens_details_unknown_property`; no second slot, retry, fallback, alternate, or accepted proposal occurred. Evidence is 2,279 bytes/SHA-256 `711cb320634c8606768aa860a43a8109d5dd89c80d303b9f4d37dfe9c678d398`; receipt is 793 bytes/SHA-256 `6d7d576737c4d727bda20b52e90904afcb5c41a23b3664c77efcca11698bab9e`; journal is 2,887 bytes/SHA-256 `8659c1d5dc25e010e0338afd0171e40c46cba4f5cee9634af8c040e0a1fb94a7`, final checksum `856017252e1734f7191f4e8f74ff363c4e79577b6df668644423ab614920dd7b`. Submission and charge are Unknown, trusted token/local-settlement counters are zero, proposal is null, response digest is nonzero, and both consumed claims exist.

This authority and every stage are consumed; no rerun or additional generation is authorized. Current official [Usage Accounting documentation](https://openrouter.ai/docs/cookbook/administration/usage-accounting) lists only `cached_tokens`, `cache_write_tokens`, and `audio_tokens` in `usage.prompt_tokens_details`, all already admitted locally. The offline correction therefore makes only that non-authoritative object name-additive while retaining the existing bounded nonnegative-integer value envelope and leaving every other strict response scope unchanged. Red-first failed only the intended valid additive case while seven malformed/boundary/unchanged-scope cases passed; the identical loop is green 8/8. Focused parser/history/vocabulary passes 99/99 including explicit JSON-token-limit coverage, full Snow Globe 1051/1051, OpenRouter CLI/security 100/100, Recording CLI 59/59, both builds 0/0, and diff checks are clean apart from line-ending notices. Independent deep review is FINAL GO with no unresolved P0-P3 findings. PR #152 passed required `build-test-smoke` in 2m53s and merged the correction as `43c553a`. No further provider action is included or authorized; accepted compatibility remains unproven.

## Historical location-specific raw-free unknown-property diagnostics

PR #151 split the response parser's former active `response_json_unknown_property` diagnostic across fourteen predefined response-object scopes: root, choice, message, reasoning detail, usage, prompt-token details, completion-token details, server-tool-use details, cost details, routing endpoints, routing candidate, routing attempt, error, and error metadata. Each diagnostic identified only that finite parser scope. It never incorporated a provider property name, value, dynamic path, response body, prompt, refusal text, exception message, or credential.

At that milestone, every strict allowlist and its evaluation order remained unchanged; only the typed rejection code supplied at each boundary differed. Proposal parsing continued to emit `proposal_unknown_property`. The broad `response_json_unknown_property` member remained historical-only so immutable artifacts and journals continued to validate and read back canonically without rewrite or reclassification.

Red-first Release evidence failed all fourteen fixture expectations with the former broad code, then passed 14/14 after the direct mapping. Focused parser/historical/exhaustive-vocabulary evidence passed 92/92; the full Snow Globe suite passed 1044/1044; OpenRouter CLI/security passed 100/100; shared Recording CLI passed 59/59; both relevant Release builds had 0 warnings and 0 errors; and `git diff --check` was clean apart from line-ending notices. Independent deep security/public-contract review was FINAL GO with no unresolved P0-P3 findings. That offline change did not access the provider, network, credential, runtime, retained live artifacts, or `src/societies/`. The current prompt-detail envelope above supersedes only prompt-token-detail name strictness: thirteen location scopes remain active and strict, while `response_usage_prompt_tokens_details_unknown_property` is retained as historical canonical vocabulary.

## Historical provider-completion generation 3: final unknown-property terminal evidence

The documented-usage correction merged through PR #149 as `d23a6cf`; required `build-test-smoke` passed in 4m23s. From that exact source, Release rebuilt with 0 warnings/errors, and the zero-I/O plan, authenticated preflight, paid record-once, and local validate each ran exactly once. Preflight accepted authorization `057f82366a4b9846aa2371f7de31749bc9e951bf5ba936de0990298232d54ddf`, 2,344-byte artifact SHA-256 `46a65a796fffe693324a53f46de4e4ca519ebf6928a94ca1ad8bcfc6761e017b`, and generation `g2-74906e534c14cf9255a7e41ecca9012ec941ec2e7c4b4c2c8cda359be45da9b8`.

The paid stage made one Azure-only ZDR exchange and again stopped `provider_response_rejected_response_json_unknown_property`; no second slot, retry, fallback, or alternate ran. Evidence is 2,233 bytes/SHA-256 `f295600120bce3c4ada87dc645aeb1280d56c5cd31bd3a4f6d5afbda63c584b1`; receipt is 793 bytes/SHA-256 `b4671467bc82bb1742f7a9840952392df70db847d4e259b1aa54ecbc5ffea064`; journal is 2,864 bytes/SHA-256 `54e6ddd51417023e454c3d03a02e378db923c4d9502e6174769b6ed5b168b07b`, final checksum `9389bd3abe3e3c2b657477331090530938bdc64924c0c6d7991cbb3c7b82fcf3`. Submission and charge are Unknown, trusted token and local-settlement counters are zero, proposal is null, response digest is nonzero, and both tombstones exist.

All three newly authorized generations and every stage are consumed. Exactly three Azure-only ZDR exchanges occurred across them, one per generation, with no retry/fallback/alternate and no accepted proposal. Aggregate authorization was capped at 54,000 microusd; each run dispatched only one 1,500-microusd-reserved slot, but all provider charges remain unknown. Current documented additive usage fields are now admitted only through bounded validation, yet generation 3 retained the same raw-free unknown-property family. At that historical boundary, the retained evidence could justify no further exact correction, no additional generation was authorized, and OpenRouter provider completion was blocked pending a reviewed location-specific diagnostic and fresh authority. Later sections record both completed follow-ups.

## Historical provider-completion generation 2: unknown-property terminal evidence

The native-finish correction merged through PR #148 as `c6429bc`; required `build-test-smoke` passed in 4m15s. From that exact source, Release rebuilt with 0 warnings/errors, and the zero-I/O plan, authenticated preflight, paid record-once, and local validate each ran exactly once. Preflight accepted authorization `5fa1e4853f7fb24679c3bbf7f7f0bad1783030a5c61fdfcdc01ab3d7e8d1f8a5`, artifact `0146c5c89daffac85799ccbd67a86f9a82f419c284fe78acd4b6fe3ee847fe54`, and generation `g2-c40ab7d6aa310f99608a4ee935258c1b74087fe0c3e8089b04ea79f65e57fb25`.

The paid stage made one Azure-only ZDR exchange and stopped `provider_response_rejected_response_json_unknown_property`; no second slot, retry, fallback, or alternate ran. Evidence is 2,233 bytes/SHA-256 `71953be66499f6bc0e163b12a9d3329d7eb7e96ac1d1ec924a5221a6eb11909f`; receipt is 793 bytes/SHA-256 `c593ec52d829b888829fe20a8fc0a9a75e05900fec0e307b1091eb95a9b0d0e8`; journal is 2,864 bytes/SHA-256 `bb1b94f09c1377b212e1437d789e62df27dc64077c7a24dd065f090faf88129b`, final checksum `61a29f34bb4c5972a3cde7e5204a3be024190a084e4c1acee2734504cc513957`. Submission and charge are Unknown, trusted token and local-settlement counters are zero, proposal is null, and response digest is nonzero. Both tombstones exist.

Generation 2 and all stages are consumed. The diagnostic identifies only an unrecognized JSON property and retains neither its location, name, value, provider body, prompt, refusal text, exception detail, nor secret. Current official Chat Completions documentation includes additive usage fields absent from the strict local allowlists: `usage.is_byok`, `usage.server_tool_use_details`, and prompt/completion members under `usage.cost_details`. Those current documented fields define the only permitted third-generation compatibility correction; strict unknown rejection elsewhere and all routing, ZDR, proposal, total-cost, first-terminal, no-retry/fallback/alternate, raw-free, and historical controls remain gates.

The correction admits only bounded typed forms of those documented members. `usage.is_byok` must be Boolean false; server-tool requested/executed counts must be bounded integer zeroes; `upstream_inference_cost`, `upstream_inference_prompt_cost`, and `upstream_inference_completions_cost` may be null or nonnegative numbers within both the reported cost and existing per-slot ceiling, with component totals consistent with the enclosing upstream/total cost. These values are not retained. Unknown properties elsewhere still reject. Red-first reproduced generation 2's diagnostic; final local evidence passes 16/16 new usage cases, 21/21 narrow bridge cases, 213/213 focused OpenRouter tests, 100/100 CLI/security, 1029/1029 full Snow Globe, both builds at 0 warnings/errors, and clean diff checks. Independent deep re-review is FINAL GO with no unresolved P0-P3 findings; PR #149 merged it before generation 3.

## Historical provider-completion generation 1: native-finish terminal evidence

The provider-completion implementation/local slice merged through PR #147 as `0a59339`. From that exact source, the zero-I/O plan, authenticated preflight, paid record-once, and local validate were each invoked once. Preflight accepted authorization `331ed7373ae3938d8fd13603a199b064d84b1d760836efd83ed4868846a2418c`, artifact `22ec2c1053affd5a40597804d67232b712a9b9c59de71b09469ca0b9467e404c`, and generation `g2-c8fb9caaa6c1e83cba7a553fb7b9e1744d0d847b5f0006e83ad885b8cfeb33f3`.

The paid stage made exactly one Azure-only ZDR exchange and stopped `provider_response_rejected_response_native_finish_reason_not_stop`; no second slot, retry, fallback, or alternate ran. The code identifies only the local check. It retains neither the provider value nor response body, prompt, refusal text, exception detail, or secret. The normalized documented finish-reason checks occurred earlier; proposal parsing was not reached.

Evidence is 2,249 bytes/SHA-256 `500c27a40a37e9cfaa49066ca4d293b5c337bce36003271683b21452a601d347`: one exchange, `SubmissionUnknown` / `ChargeState.Unknown`, zero trusted prompt/completion/total tokens, zero local settlement, null proposal, and nonzero response digest. Receipt is 793 bytes/SHA-256 `e6a717dd6825fd0d8d2f0973b1c798b2cd197bc64ff00dbce5630ddac6bb2abb`. Journal is 2,872 bytes/SHA-256 `fcbef4a12b3753983859a018c33ce27985e4eda3e38e819b9463582b326cf2c2`, with four records and final checksum `3004ac3665bf554fcdfd05eb78c952547ffbcd699b1f91bc575d100d1304aa58`. Both tombstones exist. Local zero settlement is not provider zero cost; charge is unresolved.

The authority and all stages are consumed. Its evidence-backed native-finish correction was implemented, independently reviewed FINAL GO with no unresolved P0-P3 findings, and merged through PR #148 before generation 2 above. Historical validation continues accepting this terminal code without rewriting the artifact.

## Historical finish admission after generations 1 and 2

The documented normalized `finish_reason` remains mandatory, string-typed, and exactly `stop`. The optional additive `native_finish_reason` may be absent or present as a string no longer than the existing 4,096-character bound; its value is non-authoritative and unretained. A present null or wrong-type member still fails closed as `response_native_finish_reason_type_invalid`; an oversized string fails earlier as `response_string_too_long`. Historical `response_native_finish_reason_not_stop` terminal evidence remains readable and valid but current parsing no longer emits that code. Usage admission additively validates only the bounded documented metadata in the generation-2 section above. Routing, structured output, trusted token/total-cost settlement and ceilings, refusal/error/logprobs, bounded I/O, first-terminal stop, no retry/fallback/alternate, financial uncertainty, secret, and raw-free evidence controls are unchanged.

## Historical raw-free parser rejection diagnostic baseline

When a response is received and the bounded local response parser rejects it with one of its closed active `response_*` or `proposal_*` codes, terminal evidence retains that exact local code as `provider_response_rejected_<parser-code>`. The same deterministic outcome is carried by the slot receipt, durable journal readback, evidence artifact and local artifact validation, production run result, and `record-once` CLI line. Every diagnosable parser throw is constructed from one typed enum whose identifiers are the wire suffixes; there is no second manual allowlist. Undefined enum values fail construction, and exhaustive coverage iterates every declared value automatically. Provider text and exception messages can never become an outcome code.

At that diagnostic milestone, the change did not relax admission or alter financial certainty. A parser rejection still stopped after the first terminal exchange with no retry, fallback, or alternate provider; recorded `SubmissionUnknown` / `ChargeState.Unknown`; retained the received-body digest but zero trusted token and local-settlement counters; and exposed no proposal. Those first-terminal and financial rules remain current. Pre-parser transport-policy failures and unexpected exchange exceptions retain the generic `provider_exchange_unknown` outcome, while HTTP terminal, timeout, cancellation, credential, and submission-unknown outcomes remain unchanged.

The journal and evidence schema identities and JSON shapes remain v1. Historical artifacts and journal records with generic `provider_response_rejected` remain canonical and valid without rewrite. In particular, the fifth-run artifact below remains byte-for-byte historical generic evidence and does not gain a retroactive parser cause.

Release evidence was red first: the eleven-case fake production-bridge matrix passed the three unchanged HTTP/pre-parser cases and failed all eight parser-diagnostic cases because the runtime returned generic `provider_response_rejected`. After the bounded change and P3 vocabulary-drift closure, the matrix plus CLI formatting passed 12/12; the focused evidence/journal/HTTP selection passed 88/88; the full OpenRouter CLI/security suite passed 80/80, including the current v2 run-and-validation path; the full Snow Globe Release suite passed 995/995; and both lab and OpenRouter CLI Release builds passed with 0 warnings and 0 errors. The exhaustive test proves every typed parser code maps diagnostically, an undefined enum value is rejected, and untyped response-looking or unrelated evidence exceptions stay generic. Independent deep security/public-contract re-review is FINAL GO with no P0-P3 findings.

At the PR #145 milestone, the finish-admission refinement removed `response_finish_invalid` from the active typed parser vocabulary and split the fail-closed decisions, in evaluation order, into `response_choice_index_invalid`, `response_finish_reason_missing`, `response_finish_reason_type_invalid`, `response_finish_reason_not_stop`, `response_choice_error_present`, `response_native_finish_reason_type_invalid`, `response_native_finish_reason_not_stop`, `response_logprobs_non_null`, and `response_refusal_non_null`. The current provider-completion correction retires only native-finish value mismatch from active emission; it preserves historical validation for that code and keeps native type/bound rejection active.

The historical red-first Release evidence failed exactly nine new production-bridge rows while ten unchanged rows passed; every actual terminal was the broad `provider_response_rejected_response_finish_invalid`. After that split, the bridge matrix passed 19/19 and parser-level finish coverage passed 14/14. Historical generic, broad finish-diagnostic, and native-non-stop artifacts and journals still validate/read back canonically, while an untyped `response_finish_invalid` exception maps to generic evidence. The readiness manifest remains schema v2 with unchanged digest `eea26a92d318a2ba102c7979d0cb44563d8bef967ae00b627bc6263ff59d759d`, unchanged CLI output, and `live_readiness=false`.

That historical refinement cannot reconstruct which of the nine conditions occurred in the immutable sixth-run response. The current correction does not reclassify that response or the consumed provider-completion generation 1 terminal.

## Zero-I/O paid-run readiness plan

CLI command `plan` remains pre-factory zero-I/O and emits one bounded raw-free line: exit 0, `trust_anchor_status=not_inspected`, `production_factory_constructed=false`, `additional_attempt_authorized=false`, and `live_readiness=false`. The current manifest v2 digest is `eea26a92d318a2ba102c7979d0cb44563d8bef967ae00b627bc6263ff59d759d`; the historical Attempt 4 v1 digest remains `d1e653468fd7a39e33ad355297adf96c48bde97e40a73f6b6c6812623553f737`. Invalid `plan` arguments remain offline.

Creation inspects all twelve requests produced by the real canonical serializer, including routing, structured output, pricing, reasoning, and parser stop/attempt admission. It also runs one cached fake-only, in-memory conformance set through the real evidence executor using the registered scripted exchange, fake credential lease, in-memory journal, and offline clock: twelve sequential successful exchanges, a one-exchange uncertain stop, a one-acquisition pre-dispatch stop, consumed-capability closure, duplicate-nonce closure, no retry/alternate call, and lease-buffer zeroing. These conformance capabilities and nonces are process-local test authority only. They are not restart-durable and create no authenticated preflight, provider request, live/durable/paid authority, account binding, production permit, financial artifact, or reusable run authority.

The completed v2 composition uses a fixed read-only 32-byte domain-separated HMAC anchor at a distinct Credential Manager target. The exact `state-anchor-provision-once --acknowledge-create-fixed-v2-state-anchor-and-initialize-offline` command was separately authorized, invoked exactly once, and exited 0. It stored the generated anchor without exposing its secret and initialized the exact fixed `%LOCALAPPDATA%\\Societies\\SnowGlobe\\OpenRouterPremiumOneShot\\v2` root with empty `authorities`, `execution-consumed`, `generations`, and `validation-consumed` directories plus `root-writer.lock` (567 bytes, SHA-256 `7827eeb9d15d1b43eeaaebe779b8d192ac3864e25fbfe2df8ea57b8572fd88aa`). Anchor identity digest is `d6eb68c6f14e32f342caee45f4c13d2398e4c0830e39d3e7cbfe478a10d9a78d`; state-contract digest is `f9d12a2c0bcfb60cc874dc49bc60462197700d681a42ae98ce4bcefd28ac8511`. The provisioning authority is consumed and the command must never be rerun. No OpenRouter credential read, provider access, paid action, fifth authority, generation, claim, evidence, or receipt occurred; v1 was not accessed. Production use remains open-existing only with no replace/delete/rotate/import/scan, no v1 fallback, exactly three v2 state operations, and the previously documented fail-closed ordering and bindings.

The preceding composition paragraph is limited to the anchor-provisioning slice; the sixth paid-run evidence below is the current authoritative settlement.

## Sixth authorization: finish-validation terminal evidence

Fresh authority permitted at most 12 sequential requests, no retries or alternate providers, and maximum aggregate `$0.018`. From merged source `80d0ab2`, Release rebuilt with 0 warnings/errors. Operator-observed plan/preflight/record/validate counts are 1/1/1/1 and durations were 149/1,268/4,913/194 ms. Durable state proves exactly one completed preflight generation, one execution claim, and one validation claim; it cannot prove the marker-free plan count or rejected duplicate invocations.

Preflight accepted authorization `16892f25b9c6fe81fcee1911295634f9cc88cf8bff633aa66678bef65fa42574`, preflight artifact `776fe3f1efe6548023328babd4391bdf58d8d5d9cd447896e6c9048eeee6c5fa`, and generation `g2-b85c3da641755eda2f07bd65978ea779ef3caebf8136537f55209d2c6f7d8e6c`. The paid stage made one Azure-only ZDR exchange and stopped terminal `provider_response_rejected_response_finish_invalid`; no second admit/dispatch, retry, fallback, or alternate occurred. This code identifies the local finish-validation family only and does not establish the provider's exact finish-reason value.

Evidence is 2,219 bytes/SHA `8ed4027c13f90eff69600c694d0f7d03bb316fd8bc936edbd66e20c4dc644366`: one exchange, `SubmissionUnknown` / `ChargeState.Unknown`, zero trusted tokens, zero local settlement, null proposal, and nonzero response digest. Validation receipt is 793 bytes/SHA `c1416abc97142356808e57f9d57369672e7845fbc5d5ba6521e487a92e3d6c41`. Journal is 2,857 bytes/SHA `9455a71f7b8e38a2aa389e09a41c139e7060de934e9446857b8902f194fb6f3f`, final checksum `8ce629857708749059669ffebf93fb31af875223870c353efac6adadd9b76688`, with exactly consume-preflight -> admit cq1/reserve 1,500 -> dispatch unknown -> complete finish-invalid. Both tombstones exist. Charge is unknown and zero local settlement is not a zero-cost claim. No provider status, exact finish value, raw prompt/response/secret, or accounting follow-up was retained. Independent deep security/evidence review is FINAL EVIDENCE GO with no P0-P3 findings. The authority and all stages are consumed; never rerun them.

## Fifth authorization: one-session terminal evidence

The fifth authority incorporated up to 12 sequential requests, no retries or alternate providers, and maximum aggregate `$0.018`. Preflight, `record-once`, and local `validate` each ran exactly once. Authorization digest `40655d2535520757b411595593deda6a714127e5366cf9afb3292aab0f3bc2d6`; generation `g2-03d00c2b41770f885e7ce27c9a4545cd9a420161b00351573fc2886b6c886df2`; manifest SHA `cd71868d349bb71b88d5b819a14e166fb520055d706f6da95025d04f33325d83`.

Evidence is 2,171 bytes, SHA-256 `7d449d77c3b82ff1984a0e1d33c3026b80566321938418b2eb363ff1aa9f1bd8`, terminal `provider_response_rejected`, exchange count 1, `cq1` only, `SubmissionUnknown`, `ChargeState.Unknown`, zero trusted prompt/completion/total tokens, zero locally settled microusd, and null proposal. Receipt is 793 bytes, SHA-256 `7f42dfd15ffe1e4134d7110a9f491f9dfaae1632d69e14180bf47dbc6056dc49`; it binds the same authority/generation/manifest/state/evidence and `additional_attempt_authorized=false`. Journal SHA-256 `b79acbe2cff7b2a72bee53db5c3663229de035f3700c5624b5d6f2a2dcffa161`, four records, terminal checksum `e3b987863489740096370a00a3b34374e16e7ab59d817e59743ddf2833f7b5b3`; sequence consume preflight -> admit slot 1/reserve 1500 -> dispatch unknown -> complete rejected, with no second admit/dispatch, append closed, and both tombstones.

The authority is consumed; never rerun any stage. Preflight performed authenticated metadata verification and produced durable authorization, but the exact metadata count is not retained. No raw prompt/response/secret was retained, v1 was not accessed, and no provider/accounting follow-up occurred. Local zero settlement is not provider zero cost; provider charge is unknown. The artifact does not retain HTTP status, finish reason, response body, redirect trace, or parser-rejection detail, so no provider-side cause is claimed. No runtime/framework Bearer cleanup proof is claimed. Independent deep review is GO with no P0-P2.

### One current next action

The reviewed Snow Globe/OpenRouter milestone was merged through [PR #125](https://github.com/dfrer/societies/pull/125) as `f021cdc`. The residual fake-only lease-zero conformance fix is commit `399adac` in [PR #126](https://github.com/dfrer/societies/pull/126); its merge completes the follow-up. Neither PR contains a `src/societies/` change or authorizes provider, accounting, credential, or live-state action.

This readiness boundary is the isolated Snow Globe lab. The former residual P3 is closed: bounded fake-only evidence retains and validates all twelve success lease-zero observations plus exact uncertain and pre-dispatch observations, rejecting missing, null, extra, false, or count-mismatched evidence. Focused Release tests pass 10/10, full SnowGlobe Release passes 885/885, CLI/security passes 70/70, both Release builds have zero warnings/errors, and independent deep security review is GO with no P0-P3. The canonical manifest/schema/digest is unchanged, and no credential, live-state, provider, network, accounting, paid, or `src/societies/` access occurred.

## Historical fourth authorization: terminal preflight, no provider access

On 2026-08-22 the user granted the exact fourth authority: up to twelve sequential requests, no retries or alternate providers, and maximum aggregate `$0.018`. Before invocation, readiness manifest/digest `d1e653468fd7a39e33ad355297adf96c48bde97e40a73f6b6c6812623553f737`, branch `feature/snowglobe-agent-lab-owner-v1` at HEAD `246304f`, dirty inventory, `git diff --check`, and unchanged `src/societies/` boundary were reconfirmed.

Exactly one `preflight` invocation occurred. It returned `OPENROUTER_FAILED code=preflight_already_attempted`, observed exit 1. Production `PreflightAsync` checks existing `preflight-failed`, runtime-authorization, and `execution-consumed` markers before `_metadataVerifier.VerifyOnceAsync`. Consequently this invocation performed no metadata GET, credential read, new activation bundle or runtime authorization, provider/API/account/inference request, paid request, charge, or new artifact/state mutation. No Attempt 4 `record-once` or `validate` invocation occurred. The fourth authority is consumed by this terminal preflight attempt; never retry it.

The fixed live-v1 tree remains immutable Attempt 3. Its hashes still match: runtime authorization `d13f4aaa9f930f27923147ef7ddcadbcefaf5938ca7217698fb8aa477a683288`, preflight artifact `60e8f0d1b07b6ed7c8f191d94add6f2187035cbc7f6d9492d95dc0ec7c7e19ad`, live evidence `af485def08d5c465807d069fc59a566c077f111b003723c0ece75b8db7732ea0`, and validation receipt `0885c1b530d7a12b27cbe6c7c5eddc6dd27957125fe1650d741f70a4c0332ba3`. Execution-consumed and validation-consumed tombstones remain. Do not move or delete this tree, use its authorization digest, invoke `record-once`, or invoke `validate` against it.

## Completed bounded attempts

Paid attempt 1 used authorization digest `346d4ce08428ca29db938a2b659a5cd3f4a05e0f0fc30ccf9ffc7d86450f4672` and preflight artifact digest `113563dd773b1520f9cb29a44b2eb3f3b30132d27b33531c885dafd0cd8e02a2`. Exactly one paid `record-once` invocation stopped after its first exchange with `provider_response_rejected`; no retry or second request occurred. Its archived raw-free terminal artifact is 2,171 bytes with SHA-256 `5114c02ec887ffd459ec254ed5e61daea7620638ec1fd7817a132c0c1238b315`; its one validation receipt digest is `569bdc87cf04adfe4d48ef4d8f755edb5047c69451c214b6cdad27c65be5ea58`.

OpenRouter account evidence for attempt 1 records Azure, HTTP 200, finish reason `stop`, 436 prompt tokens, 59 completion tokens, 27 reasoning tokens, and cost `$0.000158` (158 microusd). That provider charge is authoritative for attempt 1; the local journal remains Unknown because the response was not admitted.

Paid attempt 2 used authorization digest `11836bee33f6e3008894c2e426889b67123a0853a99304c7e5439c0a6eaa3fc2` and preflight artifact digest `f54eacfba80ee6fb9099260eacc8f286837e74e80c70f7dd8f9261854bcc1508`. Exactly one paid invocation stopped after its first exchange as `provider_timeout` under the then-current 5-second credential lease; no retry or second request occurred. Its archived artifact is 2,151 bytes with SHA-256 `0948bc8d4e6a91427ce49b32299cf2dab40128bc53d80c2968dd61c12ed0564f`; its one validation receipt digest is `2d3ea8b73ff737b1354fb384a783bbf4749299b022887581075c42ecb4cd7439`. Local evidence is `SubmissionUnknown` / `ChargeState.Unknown`, zero locally settled microusd, no proposal, and no raw prompt or response.

OpenRouter upstream-request evidence for attempt 2 records one Azure request, one attempt, and HTTP 200. No corresponding generation/accounting row was retained when checked, so its provider charge is unresolved; the local zero-settled field must not be interpreted as zero cost.

Paid attempt 3 used authorization digest `d13f4aaa9f930f27923147ef7ddcadbcefaf5938ca7217698fb8aa477a683288` and preflight artifact digest `60e8f0d1b07b6ed7c8f191d94add6f2187035cbc7f6d9492d95dc0ec7c7e19ad`. Exactly one paid invocation stopped after its first exchange as `provider_response_rejected`; no retry or second request occurred. Its retained raw-free artifact is 2,171 bytes with SHA-256 `af485def08d5c465807d069fc59a566c077f111b003723c0ece75b8db7732ea0`; its validation receipt digest is `0885c1b530d7a12b27cbe6c7c5eddc6dd27957125fe1650d741f70a4c0332ba3`. OpenRouter account evidence records Azure, 436 input tokens, 128 output tokens, finish reason `length`, approximately 22.9 output tokens/second, 3.46-second time to first token, and cost `$0.000241`. This is accounting/metadata evidence, not retained raw prompt or response content. No quality score or premium comparison was produced by any paid attempt.

## Corrected compatibility and timeout boundary

Attempt 1 exposed two local compatibility defects, not a provider retry condition: a stale UTC-calendar equality and rejection of documented additive reasoning/router metadata. Attempt 2 then exposed an undersized 5-second credential lease and a one-minute aggregate window that could not honestly accommodate twelve observed-duration exchanges. Attempt 3 proved the prior 128-token completion limit could exhaust exactly; the strict parser correctly rejected finish reason `length` instead of treating truncated structured output as authority.

The corrected contract retains exact Z timestamps and the bounded attestation window, admits only the bounded documented response additions, uses a 15-second lease and 240-second aggregate authorization, and fails before durable authority if that runtime would outlive the authenticated key expiry. A red-first advancing-clock regression proves that the old one-minute window terminated before twelve 5.24-second slots. The output-limit correction raises `max_completion_tokens` from 128 to 512, requests `reasoning.effort=minimal` and `reasoning.exclude=true`, and requires authenticated model and selected ZDR endpoint metadata to advertise reasoning support. Strict `finish_reason=stop`, request count, Azure/ZDR routing, and no-retry policy remain unchanged; the per-slot/aggregate ceilings rise to 1,500/18,000 microusd so the full token bounds are pre-authorized.

The reviewed composition baseline passed 55/55 focused, 868/868 full SnowGlobe Release, and 67/67 CLI/security with clean Release builds. The later provisioning implementation gate passed 881/881 full SnowGlobe and 70/70 full CLI/security. A dedicated real Global mutex abandonment regression in `OpenRouterPremiumWindowsStateTrustAnchorTests` proves fail-closed `state_trust_anchor_provisioning_indeterminate`, releases recovered ownership, and permits later reacquisition. The later lease-zero conformance follow-up closes the former residual P3 with focused 10/10, full SnowGlobe Release 885/885, full CLI/security 70/70, and both Release builds at 0 warnings/0 errors; independent deep security review is GO with no P0-P3. Local files and the credential anchor do not claim owning-user/admin or whole-volume rollback resistance; no anchor rotation/recovery is implemented.

Security review found and fixed two P1 issues and one P2 issue, then ended with no P0-P2. The later P3 follow-up retains every bounded fake lease-zero observation and validates the exact 12/1/1 sequences; independent deep security review returned GO with no P0-P3. There is no current credential-leakage finding.

At the pre-delivery fixed point, all OpenRouter code, tests, and documentation were dirty/untracked on `feature/snowglobe-agent-lab-owner-v1` at HEAD `246304f`; nothing had been staged, committed, pushed, or opened as a PR, and `src/societies/` was unchanged. That fixed point was later committed and published through PR #125 as recorded above. The credential intentionally remains in Windows Credential Manager and was not read or displayed. The first security review's unauthenticated public-documentation traffic remains separately disclosed in `WORKFLOW.md` and global T1 `WI-GLOBAL-2026-126`.

The production state-lifecycle gap is resolved for this milestone by the one-time anchor provisioning and empty-root initialization; `WI-GLOBAL-2026-126` remains a separate traffic-control issue.

## Historical claims and superseded action

This settlement proves three historical separately authorized paid attempts, a terminal fourth preflight, a fifth single-session paid flow with historical generic rejection evidence, and a sixth single-session paid flow that stopped after one exchange with raw-free `provider_response_rejected_response_finish_invalid` evidence. It does not prove a 12-slot premium run, a score, a local-vs-premium comparison, model quality, a winner, commercial suitability, world authority, the provider's exact finish value, or provider charge. Fifth and sixth authorities are consumed; the persistent credential remains stored and was not displayed.

The sixth-run delivery action was documentation-only. At that former handoff, no further paid run, provider request, credential action, or accounting query was authorized. The standing continuation authority above supersedes only that historical authority limit; every consumed sixth-run stage remains permanently closed.
