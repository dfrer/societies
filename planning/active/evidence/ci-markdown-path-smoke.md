# CI Markdown Path Smoke

Disposable pull-request fixture used to verify that Markdown-only changes satisfy the required `build-test-smoke` check through `git diff --check` without running the .NET/Godot pipeline.
